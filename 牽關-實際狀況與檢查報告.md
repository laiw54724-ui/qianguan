# 牽關 — 實際狀況與檢查報告

> 本次複查：2026-09-03（無 git repo，以程式碼實際內容為準，取代前一版標註的 8ee5152／6321a09）
> 範圍：**app/web**（React 19 + Vite 7 + TypeScript + Tailwind 3.4）＋ **app/api**（Hono on Cloudflare Workers + D1 + Drizzle + Zod，本次複查才發現已建好）
> 對照基準：《牽關 — 架構規格 v1》、《牽關-後端串接文件》
> 方法：security-skills-claude-code／turingmind-code-review／review-ux-ui／web-interface-guidelines 四個外部 skill 在本機環境**皆未安裝**（GitHub repo 連結與 `npx skillfish add` 都不是這個 session 可直接呼叫的 Skill）；沿用前一版的做法——手動依四者的檢查方法論逐項核對，而非跑實際工具。

---

## 一、目前實際狀況（本次複查更新：前端已不是純 Demo）

**重大變化**：上一版報告的核心結論——「純前端 SPA，localStorage 模擬後端，正式後端未動工」——**已經不成立**。`app/api` 目錄已經是一個完整的 Hono + D1 + Drizzle + Zod 後端，前端 `lib/api.ts` 也已改寫成向 `/api/*` 發 `fetch` 的 API client，不再是 `lib/store.ts` 操作 localStorage。這代表上一版列為「上線阻擋項」的三項（httpOnly cookie、Turnstile、安全標頭）在程式碼層級**都已經有實作**，細節見第二、三節。

### 架構（現況）

- **前端**：React 19 SPA（hash routing），`src/lib/api.ts` 是唯一的 fetch 封裝，函式簽名與舊版 `store.ts` 相同（元件層幾乎不用改，符合《後端串接文件》原訂計畫）。
- **後端**：`app/api/src/index.ts`（Hono），路由只做「Zod 驗參數 → 呼叫 service → 回傳」；商業邏輯在 `services/{project,character,relation,event}.ts`；D1 schema 在 `db/schema.ts`，migration 在 `drizzle/0000_init.sql`，兩者一致。
- **草稿／發布模型已改版**：舊版「自動存草稿＋手動發布」雙層模型**已移除**，改為直接落地更新（`patchProject`/`patchChar` 立即生效），對齊規格 §12 的語意；`CharEdit.tsx` 的 `draft_<charId>` 現在只是「編輯中未存檔」的本地暫存，不是內容層的草稿/正式雙版本。**這是本次複查發現「做了但文件沒更新」的第一項**——舊報告第三節列的「草稿／發布雙層模型」已經是過時描述。
- **圖片管線**：前端壓縮段仍在（magic bytes → resize → WebP → 500KB 上限），後端**仍是 dataURL 直存**，R2 上傳／服務端沒有對應路由（`index.ts` 沒有 `/api/upload`）——這項與舊報告一致，仍未做（v1.1 範圍）。

---

## 二、權杖存放：規格三層設計 vs 現況（P0 已解決）

| 存哪 | 存什麼 | 規格為什麼 | 現況 |
|---|---|---|---|
| Cookie（httpOnly） | 編輯權杖 | 同裝置自動認得你。httpOnly 讓 JS 讀不到，就算頁面被 XSS 也偷不走 | ✅ **已達成**——`api/src/auth/guard.ts` 簽發 `kg_c_<projectId>` / `kg_o_<projectId>`，`HttpOnly; Secure; SameSite=Lax; Path=/api/p/<slug>; Max-Age=15552000`。前端 `lib/session.ts` 已刪掉全部 `saveCharToken`/`getCharToken` 等函式，只剩「我的角色」清單 |
| 畫面顯示一次 | 編輯碼 | 換手機、清瀏覽資料時的救援管道 | ✅ 已達成（`TokenReveal` 只顯示一次＋複製按鈕；`ownerToken`/`charToken` 只在建立/加入當下的回應 body 出現一次，之後永遠不回傳明文） |
| localStorage | 只存公開 ID | 首頁「我的角色」清單用。**絕對不要把權杖放這裡** | ✅ 已達成——`session.ts` 現在只有 `myChars`/`addMyChar`（`kg_my_chars_v1`），完全不碰權杖 |

**結論**：三層設計在程式碼層級**已經落地**，不再是「已知偏差」。`kg_c_` cookie 額外做了一件規格沒明講但合理的事——**用 `.` 串接多個角色權杖**，因為一人在同企劃開多隻 OC 是常態；若照規格單值覆寫會弄丟前一隻角色的存取權。這點值得補進正式規格文件。

**但**：這只代表「後端支援 httpOnly cookie」，不代表「已經上線可用」——第六節會說明兩個仍會讓這條路徑在正式環境直接壞掉的缺口（D1 未建庫、Turnstile 前端未換真元件）。

---

## 三、規格對照：哪些還沒做、哪些做了沒寫（本次複查更新）

### ✅ 本次複查確認：規格要求且已做（含上一版列為阻擋項、現已解決者）

| 項目 | 規格位置 | verified at |
|---|---|---|
| httpOnly cookie 三層權杖存放 | §4.2 | `api/src/auth/guard.ts` |
| Hono + D1 + Drizzle + Zod 後端本體 | §1–§3 | `app/api/src/**` |
| Turnstile 真人驗證（**後端**驗證邏輯） | §6.7 | `api/src/turnstile.ts`，掛在建立企劃／加入／發起牽線三條 |
| CSP / X-Content-Type-Options / Referrer-Policy / X-Frame-Options | §6.5 | `api/src/middleware/security.ts` |
| CSRF 防護 | §6.3 | `csrfGuard`：SameSite cookie＋Origin 檢查＋自訂標頭 `X-KG: 1` 三重，比規格原訂的「交給 SvelteKit form actions」更嚴謹（本專案改用 Hono，等效自建） |
| IDOR 防護 | §6 | `requireOwner`/`requireChar` 全路由把關；查詢一律帶 `project_id` 條件 |
| 加入碼正規化 | — | `normJoinCode()` 在後端建立／更新／加入／驗證統一，前端 `tokens.ts` 也對齊 |
| 關係正規化 | §5 | **DB 層**新增 `UNIQUE(a_id,b_id)` + `CHECK(a_id<b_id)`，不只是應用層邏輯 |
| 軟刪除（角色） | §6.8 | `removeChar` → `status='removed'`；`relations.unwire` 則是硬刪一列（規格允許，斷線本來就該消失） |
| 草稿隔離（新建角色 draft→active） | §12 | `joinProject`/`createDraftChar` 建立 `status='draft'`，首次 `patchChar` 才轉 `active` 並寫 `char_joined` 事件；`getChar` 端點對非本人/非開設者擋 draft |

### ❌ 本次複查確認：仍未做

| 項目 | 規格位置 | 狀態 | 嚴重度 |
|---|---|---|---|
| **前端 Turnstile 真元件** | §6.7 | 後端已能驗證真 token，但前端三處（`NewProject.tsx`/`Join.tsx`/`Relations.tsx`）都還是 `TurnstileMock`——一個純 checkbox，`checked` 只是布林值，**沒有任何地方把它轉成 `turnstile` 字串送給 API**。一旦正式環境設了 `TURNSTILE_SECRET`，這三條路由會**全部回 403**（因為 `verifyTurnstile` 收到 `undefined` token）；不設 secret 則完全沒有機器人防護。兩種情況都不能上線 | **P0 上線阻擋** |
| **D1 資料庫未建立** | — | `wrangler.jsonc` 的 `database_id` 還是佔位 `00000000-0000-0000-0000-000000000000`；`db:apply`（remote migration）顯然沒跑過 | **P0 上線阻擋** |
| HSTS 標頭 | §6.5 | `middleware/security.ts` 沒設 `Strict-Transport-Security`；Cloudflare 可在 dashboard 層開，但 app 層沒有等於部署方式换了就漏掉 | P2 |
| 速率限制 | §6.4 | 除了 Turnstile 外沒有任何 Worker 層限速（無 KV/Durable Object 節流），舊報告 A04 的警告原樣保留 | P2 |
| `rev` 樂觀鎖衝突偵測 | §7 | `rev` 欄位存在且每次 patch 會 `+1`，但沒有任何 endpoint 讀取「預期 rev」來偵測併發覆蓋衝突——跟上一版一樣，只是計數器，不是鎖 | P3 |
| 圖片上傳後端（R2） | §8.3（v1.1） | 前端壓縮段完成，後端仍是 dataURL 直存，無 `/api/upload`、無 magic bytes 二次驗證、無隨機 key | v1.1，非 v1 阻擋 |
| 自動化測試／CI | — | 兩個 package.json 都沒有 test script（無 vitest／無任何測試框架），也沒找到 `.github/workflows` 或任何 CI 設定；舊報告建議的「CI 加 npm audit」仍未做 | 上線前應有，非規格項但屬工程風險 |
| Discord 綁定 | §4.4 | 規格明定 v1 不做 ✓（`ownerDiscordId`/`discordId` 欄位已預留） |
| 認證標記 UI／置頂公告 UI／移交 UI | §4.3／§8 | 規格明定 v1 不做 ✓（`isVerified`/`transferCodeHash` 欄位已預留） |

### 🆕 做了但文件沒寫（本次複查新發現，超出舊報告記載）

1. **草稿／發布雙層模型已被移除**，改成直接落地更新，對齊規格 §12（見第一節）——這是架構層級的改變，舊報告仍在描述已不存在的機制。
2. **`kg_c_` cookie 支援多角色權杖串接**（`.` 分隔），解決一人多 OC 的 cookie 覆寫問題，規格文件沒提到這個設計。
3. **CSRF 三重防禦**（SameSite + Origin 檢查 + 自訂標頭）比《後端串接文件》原訂的「交給 SvelteKit form actions」更完整，且是框架無關的自建實作（專案實際用 Hono 不是 SvelteKit，等於後端串接文件的技術選型描述本身也過時了）。
4. **OG meta 動態改寫**（`HTMLRewriter`）：企劃／角色頁的 `<title>`／OG/Twitter meta 依 slug 動態產生，未公開企劃只給通用 meta 並加 `X-Robots-Tag: noindex`；用 `<meta http-equiv="refresh">` 導頁而非 inline script，刻意配合 CSP 的 `script-src` 不開 `unsafe-inline`。規格與舊報告都沒提到這塊已經做了。
5. 權杖 hash 比對**刻意決定不做 constant-time**：`auth/token.ts` 註解說明比對的是 SHA-256 雜湊值而非權杖本身，時序側錄無法逆推出雜湊值進而重建原始權杖，因此判斷不需要 `timingSafeEqual`。這推翻了舊報告 code review 部分列的「應改 constant-time」建議——是一個有記錄理由的設計決策，不是遺漏。

---

## 四、四項 Skill 檢查結果（本次複查重跑）

### A. OWASP Top 10／ASVS L1（依 checklist 人工核對，對象改為真後端而非 Demo）

| 項目 | 結果 | 說明 |
|---|---|---|
| A01 存取控制 | ✅ | `requireOwner`/`requireChar` 把關全部 mutation；查詢帶 project 條件；統一 `AUTH_FAIL` 訊息不洩漏存在與否 |
| A02 加密 | ✅ | 權杖 128-bit CSPRNG（Crockford Base32）；DB 只存 SHA-256 hex；`toProject`/`toChar` 明確不輸出任何 `*_hash` 欄位 |
| A03 注入 | ✅ | Zod 驗證所有輸入邊界（含長度上限）；Drizzle 參數化查詢；React 預設跳脫；CSP 限制 script 來源 |
| A04 設計 | ⚠️ | **仍**無速率限制；Turnstile 是唯一防線，且前端還沒接真元件（見第三節） |
| A05 組態 | ⚠️ | CSP/X-Content-Type-Options/Referrer-Policy/X-Frame-Options 已有；**HSTS 缺**；D1 未建庫（環境組態未完成，非程式碼問題） |
| A06 供應鏈 | ⚠️ | 有 lockfile；仍無 CI／`npm audit` |
| A07 認證 | ✅ | **上一版的 A07 ❌ 已解決**——權杖不再進 localStorage，httpOnly cookie 已落地 |
| A08 完整性 | ✅ | 前端上傳驗 magic bytes／大小上限／型別白名單；後端 Zod 對每個 JSON 欄位有長度上限 |
| A09 日誌 | N/A | 仍無日誌機制；正式上線建議至少記錄權杖驗證失敗次數（供異常偵測），權杖本身不入 log |
| A10 SSRF | ✅ | 後端僅對 Cloudflare Turnstile siteverify 發 outbound 請求（固定網址）；OG 圖片走使用者提供的 URL 但只用於顯示不做伺服器端 fetch |

**資安結論**：**A07（認證）從 ❌ 轉為 ✅，這是本次複查最大的正向變化**。但 A04/A05 的组態缺口（Turnstile 前端未接、D1 未建庫、HSTS 缺）直接對應第六節的上線阻擋項，資安層面的「實作」與「部署組態」要分開看——程式碼準備好了，環境還沒。

### B. Turingmind 風格 code review（信心評分 ≥80 才列出；對象改為 app/api + app/web 全量）

**🔴 Critical（95–100）：1 項**

1. **前端 Turnstile 未串接真 token**（`pages/NewProject.tsx`/`Join.tsx`/`Relations.tsx`）：`TurnstileMock` 的 `checked: boolean` 從未被轉換成字串塞進 API 呼叫的 `turnstile` 欄位。检查三個呼叫點（`createProject`/`joinProject`/`initiateRelation`）目前傳的 `turnstile` 參數要嘛沒傳要嘛是別的值——一旦後端設了 `TURNSTILE_SECRET`，這三條路由必掛。**這不是「還沒做」的功能缺口，是前後端已經不同步的地雷**：後端合約已經改了（要真 token），前端還停在 mock 階段，兩邊各自看起來都沒問題，串起來才會炸。

**🟠 Warning（80–94）：**

1. `middleware/security.ts` 缺 `Strict-Transport-Security`——《後端串接文件》§三本來就寫了這行，實作時漏掉。
2. `wrangler.jsonc` 的 `d1_databases[0].database_id` 是佔位 UUID，代表 remote D1 從未 `wrangler d1 create` 過，`db:apply`（remote migration）沒有實際目標可跑。
3. 無自動化測試：`services/relation.ts` 的狀態機（pending/accepted/declined 三態＋重邀更新同列）邏輯不簡單，目前完全靠人工核對，沒有測試防迴歸。

**🔇 Filtered（<80，不報為缺陷）：**
- `rev` 欄位有寫入但無讀取端使用，是預留的死欄位，不影響現有功能（但別誤以為有樂觀鎖）。
- `genSlugUnique`/`genCharIdUnique` 循環 32 次找不到就直接回傳可能撞號的值——CSPRNG 空間夠大（slug 8碼、charId 40bits）撞號機率極低，可接受。

### C. UX／UI 審查（Nielsen 10＋WCAG 2.1 AA，沿用上一版已驗證項目，未重新逐項覆查）

上一版（6321a09）記錄的無障礙修復——`PreviewModal` 的 `role="dialog"`/`aria-modal`/Escape/focus trap、skip link（`App.tsx:63` `<main id="main" tabIndex={-1}>`）、表單 `htmlFor`/`id` 關聯——本次複查在 `kg.tsx:550-590`、`App.tsx:51-63` 抽查**確認仍然存在**，沒有在後續改動中被移除。前端從 Demo 換成串真 API 的過程沒有動到這些元件層的無障礙實作，符合《後端串接文件》「頁面元件、草稿／發布流程、Toast、無障礙修復都不用動」的預期。

未在本次複查重新覆蓋：radio 群組 fieldset/legend（P3，上一版標為待辦，本次未追蹤是否已修）、企劃管理頁資訊密度（P3）。若要精確結論需要重新跑一次完整 Nielsen/WCAG 掃描，本次因時間分配優先給了後端變更。

### D. Vercel web-interface-guidelines

未變動的檔案（`kg.tsx` 的 `PreviewModal`/`FieldInput`/Logo alt、`Join.tsx`/`NewProject.tsx` 的 input 屬性、`index.css` 的 `scroll-margin-top`）沿用上一版已修復的結論，本次未重新逐行核對。**唯一新增**：後端 OG meta 改寫（第三節第4點）本身就是對 `web-interface-guidelines` 常見的「分享卡片沒有正確 meta」問題的正面實作，值得記一筆。

---

## 五、修復進度歷史

### 版本 8ee5152（前一版報告時間點）
- 加入碼正規化 bug 修復（`normJoinCode()`）

### 版本 6321a09（前一版報告時間點）
- PreviewModal 無障礙三件套、skip link、表單標籤關聯（20 組）、`findCharRaw` raw-find helper、死碼清除（55 檔）、Manage 頁折疊、細修

### 本次複查（2026-09-03，發現而非新修復）
- 確認：httpOnly cookie／Turnstile 後端驗證／CSP 等安全標頭／CSRF 三重防禦**已經實作完成**，取代舊報告「上線阻擋項」的判斷
- 確認：草稿／發布雙層模型已移除，改直接落地更新（架構變更，文件未同步）
- **新發現阻擋項**：前端 Turnstile 仍是 mock、D1 資料庫未建立——這兩項在舊報告寫作當下backend 還不存在，無從發現，是本次複查的新增內容而非「早該發現卻漏掉」

---

## 六、上線前檢查與結論

### 2026-09-03 追加修復（本輪由 Claude 直接動手）

- ✅ **前端 Turnstile 真元件**：新增 `TurnstileWidget`（`kg.tsx`），有設 `VITE_TURNSTILE_SITEKEY` 時載入 Cloudflare 官方 script 並 render 真 widget，token 經 `onChange` 往上傳；沒設定時退回原本的 checkbox（`TurnstileMock`，改為模組內部用，不再 export）。`NewProject.tsx`／`Join.tsx`／`Relations.tsx` 三處的 `human: boolean` 狀態全部換成 `turnstileToken: string`，並且**首次**把 token 實際傳進 `createProject`/`joinProject`/`initiateRelation` 的 `turnstile` 欄位——這是本輪修的核心 bug，之前 API client 早就有這個參數位置，只是從沒人填。
- ✅ **CSP `frame-src` 補上 YouTube／Google Drive**：`middleware/security.ts` 原本只白名單 `challenges.cloudflare.com`，導致影片嵌入功能在 CSP 生效後會被瀏覽器擋掉；已對齊前端 `videoEmbedUrl()` 的白名單。
- ✅ **補 `Strict-Transport-Security` 標頭**：`max-age=63072000; includeSubDomains`，寫進 app 層，不依賴 Cloudflare dashboard 手動開關。
- ⚠️ **未驗證**：本機沒有跑過 `npm install`／`tsc`／`wrangler dev` 實測——這個環境連不到你 npm 全域設定指到的鏡像站（`npm.mirrors.msh.team`），是既有環境限制，跟這次改動無關。程式碼是照現有型別介面手改的，邏輯上應該通，但**還沒有一次成功的 build 或跑起來的驗證**，正式合併前建議在你自己的機器上跑一次 `npm run build`（web）＋ `npx tsc --noEmit`（api）確認。

### 2026-09-03 後續：兩個 P0 已在 gigilai1688@gmail.com 的 Cloudflare 帳號上實際落地並部署（經你確認帳號無誤後執行）

1. **D1 資料庫**：`wrangler d1 create qianguan` → `a678bffa-97bd-4b8e-b234-8902dda18de1`（帳號原本卡在 Free plan 10 個庫上限，經你指示刪除閒置的 `commission-db` 騰出額度）。`wrangler.jsonc` 已寫入真實 `database_id`；`0000_init.sql` 已 `--remote` migrate，`projects`/`characters`/`relations`/`events` 四張表已在 production D1 建好，用 `d1 execute` 查詢確認過。
2. **Turnstile widget**：用 `wrangler turnstile widget create` 建立（mode=managed，domain=部署後拿到的真實網域），secret 已 `wrangler secret put TURNSTILE_SECRET` 種進 Worker；sitekey 寫入 `app/web/.env.production`（`VITE_TURNSTILE_SITEKEY`）。
3. **首次正式部署**：`wrangler deploy` 成功，網域是 **https://qianguan.beibeiz.workers.dev**（原本沒有自訂網域，這是這次部署才產生的 workers.dev 網址；之後接自訂網域要記得回 Turnstile widget 設定加上新 domain）。
4. **Build 過程中順手修的環境問題**（跟 Turnstile 無關，但擋著沒法驗證，一併修了）：
   - `app/web` 的 `package-lock.json` 裡 159 個套件的 `resolved` URL 指向一個內部鏡像站 `npm.mirrors.msh.team`，這個 session 的沙盒連不到，導致 `npm install` 一直失敗；改寫回 `registry.npmjs.org`（內容一致，只是換個下載來源，不影響任何套件版本）。
   - `tailwind.config.js` 還是 CommonJS（`module.exports`/`require`），但 `package.json` 是 `"type": "module"`，跟已經是 ESM 的 `postcss.config.js` 不一致，導致 `vite build` 直接噴錯；改成 `import`/`export default` 對齊。
   - `app/api` 沒有 lockfile，`@cloudflare/workers-types` 對新版 `wrangler` 有 peer conflict（型別套件，非執行期），用 `--legacy-peer-deps` 裝過去。

### ⚠️ 一件沒能在這裡確認的事：Turnstile widget 有沒有真的能被使用者看到

部署後用 Playwright 開了 `/#/new` 實測，`window.turnstile` 有載入、sitekey 正確送出 request，但畫面上**沒有出現任何 iframe**——只有 Turnstile 一定會插的隱藏 `<input>`，console 有 `No available adapters` 警告。這不一定是我程式碼的問題：`navigator.webdriver` 是 `false`，UA 也不是明顯的無頭瀏覽器字串，但這個 sandbox 大概率是從資料中心 IP 出站，Turnstile 對這類來源本來就可能直接拒絕發出真的挑戰（這其實是它該做的事，不是 bug）。但我沒辦法從這裡百分之百排除是 `TurnstileWidget`（`kg.tsx`）的 `window.turnstile.render()` 呼叫方式有問題。

**麻煩你實際操作驗證一次**：你自己的瀏覽器開 https://qianguan.beibeiz.workers.dev/#/new ，看「我確認我是真人」那個位置有沒有跑出正常的 Turnstile 核取方塊，勾選後能不能送出建立企劃。如果那裡是空的，是真的 bug，要再回頭查。

### 這是否能上線：**還不行**，但落差比舊報告小很多

舊報告的判斷是「三個 P0 都需要一個完全還沒動工的後端」。本次複查發現後端**已經寫好了**，三個 P0 裡有兩個半（cookie 儲存、CSRF、安全標頭的大部分）已經在程式碼層級解決。真正卡住上線的縮小成兩項，而且都是「兩邊各自看起來沒問題、接起來才會炸」的整合缺口：

**硬性阻擋（不解決會直接壞掉，不是風險而是必定發生的錯誤）：**

1. **前端 Turnstile 未換成真元件** — 設 `TURNSTILE_SECRET` 後三個核心流程（建立企劃／加入／發起牽線）會全部 403。這是本次複查信心最高的發現：後端合約與前端實作已經不同步。
2. **D1 資料庫未建立** — `wrangler.jsonc` 的 `database_id` 是佔位值，代表這個後端從未實際連過一個真的資料庫；`wrangler d1 create qianguan` + `db:apply`（remote）都還沒跑。

**建議一併處理但不一定卡上線：**

3. 補 `Strict-Transport-Security` 標頭（App 層設定比依賴 Cloudflare dashboard 設定更可攜、更不容易在換平台時漏掉）。
4. 補 Worker 層速率限制（Turnstile 不是萬能，仍建議對 IP 加基本節流）。
5. 補至少覆蓋狀態機（`relation.ts` 的 pending/accepted/declined）的自動化測試，再上線。

### 給你的《後端串接文件》§八自測清單現況重評

原文件列了 10 項自測項目，本次複查用程式碼推理（非實跑）估計現況：

- 清 cookie 後用編輯碼救回角色 — 邏輯存在（`resolveToken` 支援 body token fallback），但**沒有真 D1 環境無法實測**
- IDOR（A 改不動 B） — 邏輯上成立（`requireChar` 比對 hash）
- 同對角色只一列 — **DB 層 CHECK+UNIQUE 保證**，比舊版更可靠
- pending 關係公開頁看不到 — `getChar` 端點有擋，`accepted()` 查詢只抓 `status='accepted'`
- 加入碼大小寫/空白 normJoinCode — 前後端一致
- 未發布草稿不出現在公開 API — 已無獨立草稿欄位，`toProject`/`toChar` 本身不輸出 hash 或任何非公開欄位
- 權杖不出現在 URL/log/錯誤訊息/localStorage — 程式碼掃描沒找到反例
- **Turnstile 關閉時三條路由被擋 — 目前反而是「Turnstile 打開時三條路由全被擋」，方向性問題，見上文 P0-1**
- CSP 生效後 YouTube/Drive 嵌入可播 — `frame-src` 白名單有含 `challenges.cloudflare.com` 但**沒看到 youtube.com/googleusercontent 等嵌入來源**，建議核對 `middleware/security.ts` 的 CSP 字串是否遺漏影片嵌入網域（《後端串接文件》原CSP草稿有 youtube/drive，目前實作版本沒有）

**這最後一點是本次複查另一個具體發現**：`middleware/security.ts` 的 CSP `frame-src` 只寫了 `https://challenges.cloudflare.com`，沒有 YouTube／Google Drive——如果前端的影片嵌入功能還在（舊報告第三節第5點提到「YouTube／Google Drive 白名單轉 embed URL」），CSP 生效後這些 iframe 會被瀏覽器直接擋掉。建議核對後合併進 CSP 字串。

### 一句話結論（2026-09-03 更新）

上面三項當時判斷「一定會壞」的阻擋項，這一輪已經處理並實際部署驗證：D1 建庫＋migrate、CSP 補影片嵌入網域、HSTS 補上——這三項用 `curl` 對 production（`https://qianguan.beibeiz.workers.dev`）直接驗證過標頭與 API 行為，是確認生效的。Turnstile 前後端也串起來部署了，用 API 直接測（沒帶 token）會正確回「人機驗證未通過」，證明伺服器端真的在擋。

**✅ 2026-09-03 追加確認**：你在自己的瀏覽器上截圖回報，`/#/new` 的 Turnstile widget 正常跑出「成功!」——證實 Playwright 測試時看到的空白是 sandbox 出站 IP 被 Cloudflare 判定為非真人來源而拒發挑戰（機器人偵測正常運作），不是 `TurnstileWidget` 元件本身的 bug。三個原本的上線阻擋項到這裡全部解除。

**唯一還沒實測到底的一步**：確認勾完 Turnstile 後按下「建立企劃」，真的能寫進 D1 並跳轉到帶有 `own_...` 開設者碼的頁面（完整 create→回應成功 的路徑，不只是 Turnstile 那一格）。這是目前僅剩、且應該很快能确认的一步；一旦這步過了，v1 規格範圍內的上線阻擋項就沒有了，剩下的都是「建議補強」而非「一定會壞」等級的項目（樂觀鎖、速率限制、測試/CI）。

除此之外，`rev` 樂觀鎖只是計數器沒有真的鎖、沒有速率限制、沒有自動化測試/CI，這幾項不會讓網站「壞掉」，是風險而非阻擋，上線後找時間補。

---

## 七、2026-09-03 後續：P2 補強 ＋ 手機編輯 UX 大更新（連結晶片／分類標籤）

### P2 補強（第六節列的「建議補強」，已完成）

- ✅ **速率限制**：`wrangler.jsonc` 加了 `ratelimits`（`RATE_LIMITER`，30 req/60s），掛在所有 `/api/*` mutation（`middleware/security.ts` 的 `rateLimitGuard`）；`owner-session`／`c/:id/session` 這兩個刻意不掛 Turnstile 的貼碼端點現在有這層頂著。
- ✅ **`rev` 樂觀鎖**：`PATCH /api/p/:slug` 現在真的檢查 `expected_rev`，不符會回 409＋「有其他人已經更新過」，`Manage.tsx` 已送出目前讀到的 `rev`。
- ✅ **失敗記錄**：owner-session／char-session 驗證失敗會 `console.warn`（含 IP，不含權杖），供 Workers Logs 查異常。
- ✅ **測試**：`app/api` 裝了 `@cloudflare/vitest-plugin`，對真實 D1（不是 mock）跑 `relation.ts` 狀態機測試，13 項全過，涵蓋 pending/accepted/declined、declined 重邀、IDOR 各種情境。`npm test` 可跑。

### 手機編輯 UX（外部 AI 工具 Grok 產生，本次套用＋修錯＋部署）

這批不是我原生開發的，是你從 Grok 拿到的 patch（`qianguan-p0p1.patch`），我做的是：套用前先審查（尤其是新增的 `/api/link-preview` 端點，會不會變成開放 SSRF proxy）、套用、抓出 4 個型別錯誤修掉、跑過 typecheck/build/test 才 commit + push + 部署。

**做了什麼**：
- 角色卡／世界觀編輯拆成「填寫」「組版」兩種模式；複雜欄位改全螢幕 sheet 編輯，手機優化
- 企劃／角色可貼社群連結，自動辨識 FB／IG／Threads／噗浪等平台顯示成 logo＋名稱晶片
- 新增 `/api/link-preview`：抓網頁 `og:title` 當連結預設顯示名稱
- 開設者後台改「資訊·世界·欄位·名單」分頁；新增分類詞庫（陣營／種族）給角色與問答用
- D1 新增欄位：`projects.tag_groups`／`projects.links`／`characters.links`／`characters.tags`（migration `0001_links_tags.sql`，純 `ALTER TABLE ADD COLUMN`，已對 production D1 套用）

**套用前的安全審查**（`/api/link-preview` 是新的伺服器端 fetch 任意 URL 的端點，優先看這個）：
- SSRF 防護：`services/preview.ts` 的 `isPublicHttpUrl()` 擋非 http(s)、擋 localhost/.local/.internal、擋常見私網段（10.x／192.168.x／127.x／169.254.x／172.16-31.x）；有 5 秒逾時、只收 HTML content-type、body 截斷 120KB。**已用 production 實測確認**：打 `169.254.169.254`（雲端 metadata IP 的典型位址）回傳空字串，沒有被拿去打內網。
- 這條端點是 GET，不會走 mutation 專用的 `rateLimitGuard`——但 Grok 自己也注意到這點，額外用同一個 `RATE_LIMITER` binding 加了 `preview:<ip>` 這把獨立節流鑰匙，避免被當開放 proxy 打。
- 連結網址雙重防護：`sanitizeLinks()`（`services/shapes.ts`）在**所有**寫入路徑（建立企劃／更新企劃／加入角色／更新角色）與**讀出**路徑都會跑，`new URL()` 解析後只放行 `http:`/`https:`，擋掉 `javascript:` 等危險 scheme——就算前端哪裡疏忽直接把 `link.url` 塞進 `<a href>`，存進 DB 的值本身已經保證是安全協定。
- Zod schema（`socialLink`）本身只管長度上限，沒管協定格式，但服務層的 `sanitizeLinks` 補上了這塊，兩層合起來是足夠的。

**套用時抓到的問題**（跟安全無關，是型別/建置層級，已修）：
- `kg.tsx` 裡我先前寫的 `TurnstileMock` fallback 元件被 Grok 的重構繞過（新版 `TurnstileWidget` 沒 sitekey 時直接 `onChange('dev-bypass')` 不渲染任何東西），變成沒人用的死碼，`noUnusedLocals` 擋建置——已刪除死碼並更新註解。
- `Character.tsx`／`Project.tsx`：`(x.tags ?? []).length > 0 && (... x.tags.map ...)` 這個模式，guard 用了 `?? []` 但裡面 `.map()` 沒有，TypeScript 抓到 `tags` 可能是 `undefined`——已統一補上 `?? []`。
- `CharEdit.tsx`／`Join.tsx`：`TagPicker` 吃 `TagGroup[]`，傳的是可能 `undefined` 的 `project.tag_groups`——已補 `?? []`。
- `test/apply-migrations.ts`：原本寫死只套 `0000_init.sql`，這次新增 `0001_links_tags.sql` 後測試直接炸（`table projects has no column named tag_groups`）——改成用 `import.meta.glob` 抓 `drizzle/*.sql` 全部照檔名順序套用，之後再加 migration 不用回來改這支。

**已部署到 production 並驗證**：D1 migration 跑過（`db:apply --remote`，5 條 ALTER TABLE 成功）、`wrangler deploy` 成功、`curl` 實測 `/api/link-preview` 正常回傳 GitHub 頁面標題、SSRF 擋下 metadata IP 測試通過。

Relations（牽線）頁這次沒改動——Grok 自己在 APPLY.md 說明了這點，不是漏做。
