# 牽關 — 後端串接文件

> 目的：把目前的前端 Demo（localStorage 模擬）接上規格 v1 的正式架構（SvelteKit 2 + Cloudflare D1 + Drizzle + Zod + Turnstile）。
> 對應前端版本：6321a09 ｜ 日期：2026-09-03

---

## 〇、一句話總覽

前端 Demo 的 `src/lib/store.ts` 已經把全部商業邏輯收在一處（對應規格 §2 的 service 層精神）。串接後端 = **把 store.ts 的每個 exported function 換成一條 API 呼叫**，service 邏輯整批搬到 SvelteKit 後端，前端頁面幾乎不用動。唯一要拆掉重來的是**權杖存放**（見第一節，上線阻擋項）。

---

## 一、P0-1：三層權杖存放落地（上線阻擋項）

### 目標（規格 §4.2）

| 存哪 | 存什麼 | 為什麼 |
|---|---|---|
| Cookie（httpOnly） | 編輯權杖 | 同裝置自動認得你。httpOnly 讓 JS 讀不到，就算頁面被 XSS 也偷不走 |
| 畫面顯示一次 | 編輯碼 | 換手機、清瀏覽資料時的救援管道 |
| localStorage | 只存公開 ID | 首頁「我的角色」清單用。**絕對不要把權杖放這裡** |

### 做法

1. **簽發**：權杖驗證成功的 API 回應（建立企劃、加入企劃、認領角色、手動貼權杖驗證）由後端設 cookie，**不回傳權杖明文給前端 JS 保存**：

   ```ts
   // SvelteKit endpoint 內
   cookies.set(`kg_c_${projectId}`, charToken, {
     httpOnly: true,
     secure: true,
     sameSite: 'lax',
     path: `/p/${slug}`,          // 規格 §4.2：scope 到企劃路徑
     maxAge: 60 * 60 * 24 * 180,  // 15552000 秒 = 180 天
   });
   // 開設者權杖同理：kg_o_<projectId>
   ```

2. **驗證**：每個需要權杖的 endpoint 從 `cookies.get()` 讀，sha256 後比對 DB 的 hash。前端**完全不經手**權杖。

3. **編輯碼救援**：權杖只在建立／認領當下的回應 body 出現一次（前端 `TokenReveal` 已實作「只顯示一次」UI）。換裝置時使用者在 TokenGate 貼編輯碼 → 後端驗證 → 設 cookie。

4. **前端要刪的東西**（`src/lib/session.ts`）：
   - 刪除 `saveCharToken` / `getCharToken` / `saveOwnerToken` / `getOwnerToken`（整個 `kg_session_v1`）。
   - 保留 `myChars` / `addMyChar`（`kg_my_chars_v1` 本來就只存公開 ID，符合規格）。
   - 各頁面的 TokenGate 改成「貼編輯碼 → POST 驗證 → 後端設 cookie → 重整狀態」。
   - 判斷「我是不是擁有者」改由 API 回應告訴前端（例如 `GET /p/<slug>` 回 `{ viewer: { isOwner, myCharIds } }`），前端不再自己拿權杖去試。

> ⚠️ Demo 現況：權杖存 localStorage（`kg_session_v1`）是純前端限制的已知偏差，正式上線前**必須**完成本節，否則規格 §6 的 XSS 防線少一半。

---

## 二、P0-2：Turnstile 真人驗證（上線阻擋項）

前端三處已留位置（`TurnstileMock` checkbox）：建立企劃 `/new`、建立角色（加入企劃）、發起牽線。

1. 前端把 `TurnstileMock` 換成 Cloudflare Turnstile widget，取得 `cf-turnstile-response` token。
2. 三條 API 在後端驗證：

   ```ts
   const ok = await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify', {
     method: 'POST',
     body: new URLSearchParams({ secret: env.TURNSTILE_SECRET, response: token, remoteip: ip }),
   }).then((r) => r.json());
   if (!ok.success) throw error(403, '企劃不存在或權杖錯誤'); // 統一失敗訊息，規格 §6.9
   ```

3. 規格 §6.7：這三條沒過 Turnstile 一律拒絕，防機器人洗企劃／洗角色／洗牽線。

---

## 三、P0-3：安全標頭（上線阻擋項）

SvelteKit `hooks.server.ts` 統一加（規格 §6.5）：

```ts
export async function handle({ event, resolve }) {
  const res = await resolve(event);
  res.headers.set('Content-Security-Policy',
    "default-src 'self'; img-src 'self' data: https://img.<domain>; media-src 'self' https:; " +
    "frame-src https://www.youtube.com https://www.youtube-nocookie.com https://drive.google.com; " +
    "style-src 'self' 'unsafe-inline'; script-src 'self'"); // script 不可 unsafe-inline
  res.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin');
  res.headers.set('X-Content-Type-Options', 'nosniff');
  res.headers.set('Strict-Transport-Security', 'max-age=63072000; includeSubDomains');
  // unlisted 企劃（規格 §6.4）：页面 response 加 X-Robots-Tag（取代 Demo 的 meta robots）
  return res;
}
```

- CSP 的 `frame-src` 白名單對應前端 `videoEmbedUrl()` 已做的 YouTube／Drive 限制，兩邊要一致。
- CSRF：一律用 SvelteKit form actions（自帶 SameSite cookie＋origin 檢查，規格 §6.3）；JSON API 則檢查 `Origin` header。

---

## 四、API 對照表（store.ts → endpoint）

前端 `src/lib/store.ts` 每個 export 對應一條後端路由。規格 §2：路由層只做「驗參數（Zod）→ 呼叫 service → 回傳」，商業邏輯全部在 service（可直接搬 store.ts 的實作，把 localStorage 換成 Drizzle 查詢）。

| store.ts function | 方法＋路由 | 認證 | Turnstile |
|---|---|---|---|
| `listPublicProjects` | `GET /api/projects` | – | – |
| `getProject` | `GET /api/p/[slug]` | cookie（決定 viewer 身分與是否回草稿狀態） | – |
| `createProject` | `POST /api/projects` | – | ✅ |
| `verifyOwner`（進後台） | `POST /api/p/[slug]/owner-session` | 貼 own_ 碼 → 設 cookie | – |
| `saveProjectDraft` | `PATCH /api/p/[slug]/draft` | kg_o_ cookie | – |
| `publishProject` | `POST /api/p/[slug]/publish` | kg_o_ cookie | – |
| `updateProject`（舊直改，可廢） | 併入 draft/publish | – | – |
| `removeCharacter` | `DELETE /api/p/[slug]/chars/[charId]` | kg_o_ cookie | – |
| `joinProject` | `POST /api/p/[slug]/join` | 驗 join_code（body，不比對 cookie）→ 設 kg_c_ cookie | ✅ |
| `verifyCharToken` | `POST /api/p/[slug]/c/[charId]/session` | 貼 chr_ 碼 → 設 cookie | – |
| `saveCharDraft` | `PATCH /api/p/[slug]/c/[charId]/draft` | kg_c_ cookie | – |
| `publishCharacter` | `POST /api/p/[slug]/c/[charId]/publish` | kg_c_ cookie | – |
| `createDraftCharacter`（幫別人開卡） | `POST /api/p/[slug]/c/[charId]/draft-char` | kg_c_ cookie | – |
| `initiateRelation` | `POST /api/p/[slug]/c/[charId]/relations` | kg_c_ cookie | ✅ |
| `respondRelation` | `POST /api/relations/[id]/respond` | kg_c_ cookie（且須為被邀請方） | – |
| `updateRelationSide` / `updateRelationExtras` | `PATCH /api/relations/[id]` | kg_c_ cookie（限雙方） | – |
| `unwireRelation` | `DELETE /api/relations/[id]` | kg_c_ cookie（限雙方） | – |
| `feed` | `GET /api/p/[slug]/feed?before=<ts>` | – | – |
| `rosterStats` / `listCharacters` | `GET /api/p/[slug]/roster` | – | – |
| `findSimilarProjects` | `GET /api/projects/similar?title=` | – | – |

### 必須原樣保留的規則（Demo 已實作，搬時別弄丟）

- **統一認證失敗**：任何權杖錯誤都回「企劃不存在或權杖錯誤」＋ 404/403 一致行為（§6.9）。
- **IDOR**：每條查詢都帶 project 條件；關係操作驗 `sideOf`（限當事雙方）。
- **權杖**：`chr_/own_/inv_` 前綴＋128-bit CSPRNG＋Crockford Base32；DB 只存 SHA-256 hex；權杖永不進 URL、log、錯誤訊息。比對用 constant-time（`timingSafeEqual`）。
- **加入碼**：雜湊前一律 `normJoinCode()`（`trim().toLowerCase()`）——Demo 已在建立／更新／發布／驗證四處統一，後端照做。
- **關係正規化**：`a_id < b_id`＋`UNIQUE(a_id, b_id)`；declined 重邀更新同列（§5）。
- **軟刪除**：`status='removed'/'declined'`，不硬刪（§6.8）。
- **草稿隔離**：所有公開查詢在 service 出口剝除 `draft`（Demo 的 `publicProject`/`publicCharacter` 模式）；草稿欄位直接不 SELECT 或回應前剔除。
- **pending 隱私**：pending/declined 關係只出現在雙方視角，公開名單／動態牆只算 accepted。

---

## 五、D1 DDL 對應

`src/lib/types.ts` → 資料表（規格 §3）：

- `projects`：id, slug UNIQUE, title, summary, cover_url, icon_url, visibility, join_mode, join_code_hash, signups_open, is_verified, announcement, field_schema(JSON TEXT), world_blocks(JSON), qa(JSON), owner_token_hash, draft(JSON，對應 ProjectDraft), rev, status, created_at, updated_at
- `characters`：id, project_id FK, name, one_liner, avatar_url, profile(JSON), blocks(JSON), edit_token_hash, draft(JSON), status, created_at, updated_at
- `relations`：id, project_id FK, a_id, b_id, a_label, a_note, b_label, b_note, extras(JSON), initiator, status, created_at, updated_at；**CHECK(a_id < b_id)**＋**UNIQUE(a_id, b_id)**
- `events`：id, project_id FK, type, actor_id, target_id, payload(JSON), created_at；feed 查詢 `WHERE project_id=? AND created_at<? ORDER BY created_at DESC LIMIT 30`（cursor 分頁，前端已實作「載入更早的動態」）

profile 用 JSON 欄，不要 EAV（§3）。索引：`(project_id, created_at)` on events；`(project_id, status)` on characters。

---

## 六、圖片上傳（規格 §8.3，v1.1）

前端段已完成（magic bytes 驗證 → createImageBitmap 壓長邊 ≤1200px → WebP q0.82 → >500KB 退回 → HEIC 明確錯誤訊息）。後端段：

1. `POST /api/upload`（Worker）：**再驗一次 magic bytes**（不信任前端），壓縮後仍 >500KB → 415 退回。
2. 存 R2：key 用隨機值 `img/<random22>.webp`，**不用原檔名**；替換時刪舊物件。
3. 服務時強制標頭：`Content-Type: image/webp`、`Content-Disposition: inline`、`Cache-Control: public, max-age=31536000, immutable`、`X-Content-Type-Options: nosniff`；走獨立 `img.<domain>` 子網域。
4. 濫用防護：開設者可移除任何圖；上傳記 `SHA-256(IP + 每日鹽)`；檢舉信箱；每角色／每企劃配額。
5. 前端接上時把 `files.ts` 的 dataURL 改為上傳後回傳的 URL；localStorage 塞 dataURL 的 `StorageFullError` 路徑自然消失。

---

## 七、前端要改的檔案清單

| 檔案 | 改動 |
|---|---|
| `lib/session.ts` | 刪權杖三函式，留「我的角色」清單 |
| `lib/store.ts` | 整檔改寫成 API client（函式簽名不變，內部換 `fetch`） |
| `components/kg.tsx` TokenGate | 貼碼 → POST 設 cookie；不再 `saveXxxToken` |
| `components/kg.tsx` TurnstileMock | 換真 Turnstile widget |
| 各頁 `isOwner`/`mineHere` 判斷 | 改吃 API 回的 `viewer` 資訊 |
| 圖片上傳 | dataURL → 上傳 API（見第六節） |

頁面元件、草稿／發布流程、Toast、無障礙修復**都不用動**。

---

## 八、上線前自測（規格 §10 ＋ 本次新增）

- [ ] 清掉 cookie 後，用編輯碼能救回角色（三層存放成立）
- [ ] A 的編輯碼改不動 B 的角色（IDOR）
- [ ] 同一對角色只產生一列關係（a_id<b_id＋UNIQUE）
- [ ] pending 關係在公開頁看不到
- [ ] 加入碼「Fog2026」建立後，用「fog2026」或前後帶空白也能加入（normJoinCode）
- [ ] 未發布的草稿在任何公開 API 回應中不存在（含 network 面板直接看 JSON）
- [ ] 大寫/空白加入碼舊資料遷移：若已有用未正規化碼雜湊的企劃，重新雜湊一次
- [ ] 權杖不出現在任何 URL、log、錯誤訊息、localStorage
- [ ] Turnstile 關閉時三條路由都被擋
- [ ] CSP 生效後 YouTube／Drive 嵌入仍可播，站外 iframe 被擋

## 九、建議移接順序

1. D1 DDL＋Drizzle schema＋種子資料（照 `store.ts` 的 `db()` seed）
2. 權杖 cookie 簽發／驗證 middleware（第一節）＋統一 AUTH_FAIL
3. 唯讀 API（project/roster/feed）→ 前端切過去
4. 寫入 API（草稿/發布/牽線）→ 前端切過去
5. Turnstile＋安全標頭＋CSRF
6. 圖片上傳（v1.1）
