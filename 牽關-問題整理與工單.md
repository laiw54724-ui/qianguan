# 牽關 — 問題整理與工單

> 2026-09-03 ｜ 對象：`qianguan.beibeiz.workers.dev` 已部署版本
> 用法：由上往下做。P0 是現在就在壞的，P1 是結構性的，P2 是新功能，P3 是細節。

---

## P0 — 現在就在壞

### 0-1 加入碼沒有被儲存

**症狀**：建立企劃時設了加入碼，之後在設定裡看不到，加入時也驗不過。

**推測原因**：拆掉草稿／發布雙層模型時漏改。原本的邏輯是「加入碼在發布時才雜湊」，發布這一步被移除後，`join_code_hash` 的寫入路徑可能還留在一個已經沒人呼叫的分支裡。

**要做**：檢查建立與更新企劃的 service，確認 `join_code_hash` 在**建立當下就寫入**。同時確認 `normJoinCode()`（`trim().toLowerCase()`）在建立、更新、驗證三處都有套用。

**驗收**：建企劃設加入碼 `Fog 2026` → 用 `fog2026`、` FOG2026 ` 都能加入；管理頁看得到「已設定加入碼」狀態。

### 0-2 `og:image` 是空的 — ✅ 已完成

~~**症狀**：HTML 外殼裡 `<meta property="og:image" content="">`，同時 `twitter:card` 宣告 `summary_large_image`——等於承諾一張大圖卻不給，比不設還糟。~~

已放好 `app/web/public/og-default.png`（1200×630）並填進 `index.html` 外殼的 `og:image`/`twitter:image`。企劃頁與角色頁各自的動態 OG（依角色/企劃內容覆寫）見 P2 第一步。

### 0-3 手機輸入時畫面自動放大

**症狀**：iOS Safari 點進輸入框時整頁縮放。

**原因**：iOS 的固定行為——輸入框字級小於 16px 就自動放大。

**要做**：

```css
input, textarea, select { font-size: 16px; }
```

桌面版要小一點再用 media query 調。**不要用 `maximum-scale=1` 或 `user-scalable=no`**，那會連帶關掉使用者自己縮放的能力，是無障礙倒退。

**驗收**：iPhone Safari 上點任一輸入框，畫面不動。

---

## P1 — 結構性問題

### 1-1 企劃層導航

**問題**：進了 `/p/:slug` 之後，建角色等頁面跳出了企劃的脈絡。在企劃內的所有操作都以企劃為單位，導航必須反映這件事。

**要做**：`/p/:slug/*` 底下共用一個殼。

- 頂部固定：企劃名稱（點擊回企劃首頁）
- 底部固定四格，**圖示 + 兩個字**（純文字在手機上會被截斷）：動態／名單／我的／設定
- 建角色、發起牽線這類動作從頁面內進入，不佔導航格
- 底部列加 `padding-bottom: env(safe-area-inset-bottom)`，否則 iPhone 的 home indicator 會蓋住

### 1-2 換頁時內容分批跳出

**問題**：切頁面時先出現空殼，內容隔一下才陸續出現，還會位移。

**要做**：兩件事一起。

1. 用 React 19 的 `startTransition` 包住路由切換——**舊畫面會留在原地直到新畫面準備好**，這正是「跑完再一起顯示」。用 `useTransition` 的 `isPending` 驅動頂部進度條。
2. 檢查請求瀑布。如果企劃頁是「先抓企劃 → 再抓角色 → 再抓關係」三輪往返，改成一個端點一次回傳，或至少三個請求並行。

**驗收**：手機上連續切換三個分頁，畫面不出現空殼閃爍。

### 1-3 動態牆改為主動發布

**問題**：「◯◯ 更新了設定」是自動追蹤，這個社群有不少人不喜歡被關注。

**決定：不做可見性開關，改掉事件本身。** 開關會變成一個要設定的東西，而且預設值怎麼選都會傷到一群人——預設開，內向的人被暴露；預設關，動態牆是空的。

- **保留**：「◯◯ 加入了」（加入是公開行為）、「◯◯ 和 ◯◯ 牽上線了」（雙方都同意過）
- **移除**：「◯◯ 更新了設定」的自動事件
- **新增**：角色頁存檔後問一句「要不要跟大家說一聲？」，可附一行字（例：「補了背景故事」）。預設不發，不想說就不說。

改完之後動態牆的內容從「有人動了什麼」變成「有人想讓你看什麼」，隱私問題自然消失，資訊量還變高。

### 1-4 編輯碼救援

> （P2 第二步會連同權杖系統一起刪除，這是預期）

**問題**：編輯碼只顯示一次，在手機上很危險——使用者按了複製，接著跳去 Discord 貼連結，剪貼簿就被蓋掉了。這會是最大的客服來源。

**要做**：

- 顯示編輯碼的畫面要勾選「我已經存好了」才能繼續
- 角色頁常駐一個「重看編輯碼」入口，用現有 cookie 驗證即可（人已經在自己的角色頁了，沒有新增風險）

---

## P1.5 — 動工 P2 之前要併的幾件事

> 2026-09-04 新增：這兩項在 P2（尤其 Discord 淨刪除權杖那一步）動工前要先定案，不然關係模型跟單人可用性會被 P2 的重構晚一步撞見。

### 1.5-1 關係卡少了一層：共用互動

實際填寫時的內容有三層，現在的模型只有兩層：

| 層 | 例 | 現況 |
|---|---|---|
| 各自視角 | 「跟著他可以吃他剩下的東西，賺爛了」 | 已有 `a_note` / `b_note` |
| 關係標籤 | 朋友 | 已有 `a_label` / `b_label` |
| 兩人共用的互動 | 「烏鴉喜歡拔狼毛，飛走再回來拔」 | 沒有 |

第三層不屬於任何一方的視角。原型裡「其他補充・雙方共用」就是它，但沒定義清楚。

```sql
CREATE TABLE relation_notes (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  relation_id INTEGER NOT NULL REFERENCES relations(id),
  body        TEXT NOT NULL,
  author_side TEXT NOT NULL,          -- 'a' | 'b'
  created_at  INTEGER NOT NULL
);
CREATE INDEX idx_rnotes ON relation_notes(relation_id, created_at);
```

**規則**：關係 `accepted` 之後才開放（`pending` 期間不能寫）；雙方都能新增，但只有新增者能刪自己那條；不做即時協作、不做編輯歷史。用條列不用自由文字。

### 1.5-2 單人也要能用完，這是優先度問題

這個站原本的構想是給自己整理用的。現在整套設計預設「要有第二個人來、要對方同意、才有東西看」——如果只有三五個人註冊，先來的人會打開一個空的站然後不回來。

所以單人路徑必須完整可用：

- 建角色、記關係、看關係圖，一個人就能全部做完
- 對方不在站上時，關係以純文字記下對方名字，只有自己看得到，不進公開頁、不進動態、不通知任何人
- 雙向同意是加值，不是前提

> **未確認的關係紀錄一律私人。**

**機制定案：不建「幽靈角色」，只記私人筆記——不要讓人幫別人建角色。**

代建是壞主意，理由：

- **佔名**：建了一個「阿楠」，這個名字就在企劃裡了。本人來的時候要嘛認領代建的版本，要嘛變成兩個阿楠。
- **替人發言**：代建的內容是你眼中的他，但掛的是他的角色名。認領之後那些字還在，同人圈對這件事很敏感。
- **重複**：A 和 B 各自幫阿楠建一隻，就有兩隻要合併，而且沒有乾淨的合併方式。
- **名單污染**：二十個人各自幫其他十九個代建，最多 380 筆幽靈列。
- **最麻煩的是持續性的規則負擔**：幽靈角色必須從名單、動態牆、關係圖、OG meta、搜尋全部排除，而且是「每次新增查詢都要記得加這個過濾」的規則，遲早會在某一處漏掉。

最後一點是決定性的：**結構上不可能發生，勝過一條要一直記得套用的規則。**

改成不建角色、只記一筆私人筆記。差別在於它宣稱了什麼——幽靈角色宣稱「這個人的角色存在於這個企劃」，私人筆記只宣稱「我跟一個我叫他阿楠的人有關係」。後者才是事實。

用**獨立的表**，不要塞進 `relations`：

```sql
CREATE TABLE private_relations (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id     TEXT NOT NULL REFERENCES projects(id),
  owner_char_id  TEXT NOT NULL REFERENCES characters(id),  -- 我的角色
  ghost_name     TEXT NOT NULL,        -- 對方的名字，純文字
  label          TEXT NOT NULL DEFAULT '',
  note           TEXT NOT NULL DEFAULT '',
  linked_char_id TEXT REFERENCES characters(id),  -- 日後對上真人時填
  created_at     INTEGER NOT NULL,
  updated_at     INTEGER NOT NULL
);
CREATE INDEX idx_priv ON private_relations(owner_char_id);
```

分開一張表有兩個實際好處：

1. `relations` 靠 `a_id < b_id` 正規化，塞一個可為空的 `b_id` 進去會直接破壞唯一索引。
2. **凡是查 `relations` 的地方，天生就是公開安全的。** 不需要記得過濾。

**轉正的流程**：對方真的加入之後，在他的角色頁或你的關係頁顯示一句：「你有 2 筆私人紀錄寫著『阿楠』，站上現在有同名角色，要送出正式邀請嗎？」按下去才轉成一筆 `pending` 的正式關係，走既有的雙方確認。名字比對只做建議，**絕不自動轉換**，也不通知對方——在他同意之前，他不該知道有人記了他什麼。

**主催能不能預先建名單？不要做。** 看起來方便，但它把整個所有權問題原封不動搬回來。

### 1.5-3 淨刪除：`draft-char`（代建角色）整條流程

跟 1.5-2 是同一個決定的另一半：既然不讓人代建角色，`draft-char` 這條路就不該存在。

**要刪掉的東西**：

- `POST /api/p/:slug/c/:charId/draft-char` 端點
- `Relations.tsx` 裡「請填對方角色的名字」那段建角色流程與對應前端狀態
- 換成：寫進 1.5-2 的 `private_relations`

這也順便解掉「無主角色」的問題——**規則變成：角色只能由本人建立，沒有例外。** P2 第二步 Discord 換掉權杖時，就不會有「這隻角色沒有 `discord_id` 該歸誰管」這種要特別處理的情況。

又是一次淨刪除，跟 P2 的方向一致。

---

## P2 — 分享優先重構

> 2026-09-04 更新：這一節取代舊版「角色分享與跨企劃牽關」——先解決一個矛盾，再排出施工順序。

### 先解一個矛盾：登入牆 vs 分享

「不開放訪客」和「角色名片能分享到噗浪」直接打架——分享出去的連結若撞上登入牆，分享就沒意義。

**規則：讀取公開，寫入登入。**

- 任何人都能看角色卡、企劃卡（unlisted 企劃除外）
- 建角色、發起牽關、編輯、加入企劃，一律要 Discord 登入

要防的是匿名亂發牽關，不是防人看。

### 第一步：hash routing 換成 path routing

優先度最高，因為它擋住「能分享名片」這個核心目標。

現在網址是 `/#/p/xxx/c/yyy`。井字號後面的東西永遠不會送到伺服器——Worker 看不到是哪個企劃或角色，所以 OG meta 寫不出來，分享出去永遠是同一張通用卡。這不是樣式問題，是路由架構問題。

**要做**：

- 前端路由改用 History API（真實路徑 `/p/xxx/c/yyy`）——核心引擎在 `app/web/src/lib/nav.ts`（`navigate`/`href`/`useHashPath`），連帶 `dirty.ts:111` 的離開攔截導覽、`App.tsx` 那個因為 hash 路由才需要的 `#main` skip-link workaround，都要跟著換掉，不是只改路由表
- Worker 對所有非 `/api/*` 的路徑回傳 SPA shell
- 接上規格 §11 的 HTMLRewriter，注入企劃／角色的 OG meta（`og:image` 預設值 0-2 已經修好，這裡只是沿用既有機制，不用重做）
- **拿掉兩處寫死的 hash 轉址，不是改字串**：`app/api/src/index.ts` 第 394、414 行，既有 §11 OG meta 邏輯（`servePage()`）裡有 `<meta http-equiv="refresh">` 把瀏覽器彈去 `` `/#/p/${slug}${charId ? `/c/${charId}` : ''}` ``——這是 hash 路由時代的補丁，因為爬蟲抓得到靜態 meta 但瀏覽器要另外導去 JS 接手的位置。**path routing 之後這個轉址整個不需要**：JS 會在同一個網址原地接手，不用再彈去別的地方。把 `HeadRewriter` 建構子傳進去的 `redirect` 參數改成 `null`，不是把字串裡的 `/#/` 換成 `/`。（掃過 `docs/archive/` 那份作廢計畫裡 OAuth callback 的 hash 轉址：那段程式碼從沒進過 main，不用管。）
- **加一段 hash → path 的相容重導**（十行內）：Worker 收到帶 `#` 的舊連結時（實務上偵測不到 fragment，這段其實是給還留著 `/#/p/xxx` 書籤或轉貼連結的使用者，用一小段前端 boot 腳本讀 `location.hash` 存在就 `history.replaceState` 轉成新路徑）轉到新的 `/p/xxx` 路徑。現在只有測試資料，成本低，之後路由已經散出去再補會麻煩很多。

**隱私規則（規格 §11）**：unlisted 企劃只給通用 meta，不得輸出真實標題與封面。角色卡即使在 unlisted 企劃底下也要能分享，但 OG 只帶角色本身的資訊，不帶企劃名稱。

**驗收**：把角色連結貼進 Discord 與噗浪，展開出角色名與頭像的卡片；手動打開一個舊的 `/#/p/xxx` 連結，確認會被轉到新路徑而不是 404。

### 第二步：Discord 登入取代權杖系統（淨刪除）

角色卡現在是跨企劃的身分、別人可以對你發起牽關——這已經是帳號形狀的產品，「不用註冊」的前提不成立了。

**要刪掉的東西（這一步做完程式碼會變少）**：

- 編輯碼 `chr_` / 開設者碼 `own_` 的產生、顯示、貼碼救援 UI
- 權杖三層存放設計、TokenGate 元件
- `user_links` 的 AES-GCM 加密與 `LINK_KEY`
- `confirmed` 欄位與待確認清單
- `kg_u` 的 HMAC 簽章
- 共用電腦誤連結的整套防護

上述全部隨著「Discord 是唯一身分來源」一起消失。Discord 整合設計文件裡的加密、`confirmed`、freshness 討論全部作廢——那些是為了「Discord 只是選配」而存在的複雜度。

**新的模型**：

```
sessions: discord_id 是唯一身分
characters.discord_id  → 誰擁有這隻角色
projects.owner_discord_id → 誰開的企劃
```

這兩個欄位早就預留在 schema 裡，現在真的派上用場。

**保留**：state 一次性存 KV、回呼轉址目標只從 state 組出（不接受查詢參數）、scope 只要 `identify`、不存 username／avatar。

同一個 Discord 帳號可以有多隻角色，`discord_id` 不要加 unique 約束。

### 第三步：導航

桌機與手機要分開。底部標籤列是手機模式，桌機用左側或頂部導航並加寬內容區。

頁首左邊的圖示改成企劃自己的封面或縮圖，沒設就用企劃名第一個字做字母標。牽關的 logo 只留在站首頁。

底部列保留 `padding-bottom: env(safe-area-inset-bottom)`。

### 第四步：首次繪製與跑版

#### 4-1 畫面在拿到資料前就先畫了

**症狀**：點編輯或設定時會閃一下「請輸入權杖」再跳走；切換企劃／角色列表時內容分批跳出。

這兩個是同一個病——畫面在還不知道自己是誰、還沒拿到資料時就先繪製。

**要做**：把首次繪製擋在一個 bootstrap 查詢後面，session 與該頁資料都回來之前只顯示骨架，不要先畫出一個馬上會被抽掉的狀態。搭配 `startTransition` 讓舊畫面留到新畫面備妥。

兩個要一起修，不要分開處理。

#### 4-2 固定頁首蓋住內容

捲動後子編輯器的標題與「完成」按鈕被企劃頁首壓住。內容容器加 `padding-top` 等於頁首高度，錨點目標加 `scroll-margin-top`；或子編輯器改成疊在頁首之上的 sheet，不共用同一條捲動流。

#### 4-3 輸入框溢出卡片（關係卡、Q&A 都是同一個毛病）

「區塊標題」「Q」的輸入框比卡片寬，把「刪除」擠到卡片外。那一列缺 `min-width: 0`——flex 子元素預設不會縮到內容寬度以下。

#### 4-4 長文編輯器是空的且不能捲動

點「點擊寫長文」跳進去只有一個白框，內容區沒有高度也沒有捲動。Q&A 的 A 同一個元件同一個症狀。這個是壞的，不是體驗問題。

**修法**：內容區給明確高度、可捲動、鍵盤彈出時輸入區吸附在鍵盤上方、「完成」正確寫回原欄位。

#### 4-5 兩條固定列吃掉螢幕

儲存列加底部導航疊在一起，小螢幕上約 25% 是介面外框。沒有未儲存變更時把儲存列收起來，只在有變更時滑出。

### 第五步：關係圖

先講實話：二十個角色的全域關係圖在 380px 螢幕上不可能好用。它的價值不在使用，在截圖分享——主催會把它貼到群裡。

所以定位成展示品，不是操作介面：

- 桌機：完整力導向圖
- 手機：預設「以我為中心」的一層放射圖 + 名單，超過 8 個退回列表
- 提供一鍵匯出成圖片
- 不要在手機上做拖曳畫布

### P2 施工順序

1. ~~path routing + OG meta（解鎖分享）~~ — 已完成並部署
2. ~~Discord 登入取代權杖（淨刪除）~~ — 已完成並部署（2026-09-04）
3. 導航拆桌機／手機、logo 換企劃的 — 見下方 Ticket-07/08
4. bootstrap 擋首次繪製 + 五項跑版 — 見下方 Ticket-02/05/06/08，內容已被實測回報取代，本節保留供對照
5. 關係圖

**不要重寫整個專案。依序改，每一步結束都要能跑。**

---

## P2.5 — 上線後實測回報（Ticket-01～17）

> 2026-09-05 新增：P2 第一、二步部署後，主催實測回報的問題與新需求，取代上面「第四步：首次繪製與跑版」的推測版本（4-2/4-3/4-4/4-5 對應到下面 Ticket-05/06/02/08，內容更新為實測結果）。用法沿用文件開頭：由上往下做，🔴 是現在真的壞的，🟠 是跑版，🟡 是體驗，🟢 是新功能，⚪ 先不做。

### 🔴 優先修復：核心 Bug 與功能異常（Critical）

#### Ticket-01　Safari Discord 登入跳針無限迴圈

**症狀**：在 Safari 用 Discord 登入後，狀態沒有正確保留，畫面一直跳回要求授權。

**要查**：這是 P2 第二步剛上線的新流程，還沒人實測過 Safari。優先懷疑 `kg_session` cookie 的 `SameSite=Lax` 在 Safari 的第三方導向（`discord.com` → 我們的 `/api/auth/discord/callback`）情境下是否真的有跟著帶回來——Safari 的 ITP（Intelligent Tracking Prevention）對「導向鏈中間插了外部網域」的 cookie 處理比 Chrome 嚴格，實測前不要假設跟 Chrome 行為一致。動手前**先用 systematic-debugging 的做法**：在 callback 路由每一步加驗證性 log（state 讀到了嗎、code 換到 token 了嗎、discord_id 拿到了嗎、session 建立了嗎、Set-Cookie 標頭實際送出了什麼），用 Safari 實機或 BrowserStack 之類工具重現一次抓出卡在哪一步，不要用「應該是 cookie 問題」直接下去改。

**驗收**：Safari（iOS 與 macOS 都測）走一次登入，落地頁 `GET /api/me` 回傳正確的 `discordId`，且重新整理不會被彈回登入。

#### Ticket-02　長文編輯器與 Q&A 空白死機（原跑版 Bug 3）— ✅ 已修復（2026-09-05）

~~**症狀**：點擊「寫長文」或 Q&A 的 A，跳出的編輯器只剩白框，沒有高度也無法捲動。這屬於功能損壞，需優先修復。~~

**實測確認的根因**：用 Playwright 開角色編輯頁重現後發現，長文的全螢幕編輯器（`FillSheet`）**完全沒有蓋滿螢幕**——外層企劃頁首跟底部導覽都還露在外面，`FillSheet` 自己的標題列跟「完成」按鈕整個不見，因為它被吃進了頁面正常捲動流裡，不是真的 `position: fixed` 蓋滿畫面。

追到的真正原因：`.kg-rise`（幾乎每個頁面區塊都掛的進場動畫）用 `animation-fill-mode: both`，動畫播完後會**永遠**定住 `to` 關鍵影格算出來的 `transform`。就算 `to` 寫的是 `translateY(0)`（視覺上等於沒有位移），瀏覽器實際定住的值是內插算出來的單位矩陣 `matrix(1,0,0,1,0,0)`，不是字面上的 `none`——而只要 `transform` 不是 `none`，這個元素就會變成子孫 `position:fixed` 元素的新包含區塊。`FillSheet` 只要巢狀在任何一個播過 `.kg-rise` 動畫的容器底下（幾乎所有頁面區塊都是），就會被困住，變成卡在頁面裡的一個普通區塊，而不是真正蓋滿螢幕的彈窗。

Q&A 的 A 欄位是另一個獨立的病，跟 Ticket-06 同源：`QaEditor` 的 A 欄位（`ImeTextarea className="kg-textarea flex-1"`）沒有 `min-w-0`，在窄螢幕（實測 320px 寬）被相鄰元素擠壓到視覺上像一格空白。

**已完成**：`.kg-rise` 動畫的 fill-mode 從 `both` 拿掉 `forwards`（改成只留 `backwards`——部分呼叫端有帶 `animationDelay`，這個還是需要），`QaEditor` 的 A 欄位加上 `min-w-0`。兩處都用 Playwright 重現、修好、再重新量測確認過（sheet 蓋滿視窗、textarea 可捲動、「完成」正確寫回內容）。

#### Ticket-03　影音連結（YouTube／Spotify）嵌入失效（新增）

**症狀**：貼上 YouTube 或 Spotify 連結後無法正常播放，可能 iframe 被擋或嵌入解析邏輯出錯。

**要查**：`kg.tsx` 的 `videoEmbedUrl()` 目前只認 YouTube／Google Drive（CSP 的 `frame-src` 也只開了 `youtube.com`／`drive.google.com`，見 `middleware/security.ts`）——**Spotify 目前完全沒有支援**，不是壞掉，是從沒做過；先確認要不要真的支援 Spotify（要的話 CSP 的 `frame-src` 要一起加 `open.spotify.com`）。YouTube 那條路才需要用 systematic-debugging 排查是不是真的壞了。

#### Ticket-04　儲存狀態誤判與效能卡頓

**症狀**：編輯後按儲存要等很久；儲存完退出時系統仍誤判成「還沒存」，跳出重複的離開警告。

**要查**：離開警告是 `lib/dirty.ts` 的 `useLeaveGuard` 機制，各編輯頁（`Manage.tsx`/`CharEdit.tsx`）的 `dirty` 是拿目前表單狀態的 `JSON.stringify` 跟 `snapshot.current` 比對——儲存成功後兩處都應該同步更新成新值，若有一處漏更新（例如非同步時序、或某個表單欄位沒被納入 `currentForm()`）就會出現「明明存了還說沒存」。「儲存太久」要先量測是網路來回慢還是前端在儲存前做了不必要的重運算（例如整包世界觀/角色卡內容在每次按鍵都重新序列化）。

### 🟠 介面跑版與顯示錯誤（Layout / UI Bugs）

#### Ticket-05　固定頁首遮擋內容（原跑版 Bug 1）

**症狀**：捲動後，頁首會蓋住編輯列、大事記標題與子編輯器頂端（吃掉一整行）。

**工程解法**：內容容器加上等於頁首高度的 `padding-top`，錨點目標加 `scroll-margin-top`；或把子編輯器改成真正疊在頁首之上的 sheet，不跟頁首共用同一條捲動流。

#### Ticket-06　區塊標題輸入框溢出（原跑版 Bug 2）— ✅ 已修復（2026-09-05）

~~**症狀**：區塊標題輸入框比卡片寬，把「刪除」按鈕擠到卡片外。~~

**實測確認的位置跟 diagnosis 原本猜的不一樣**：世界觀區塊的標題輸入框（`BlockEditCard` 組版模式的標題列）**本來就有 `min-w-0`**，不是它溢出。實際重現出來、且有一個直接可見「刪除」按鈕會被擠出卡片外的地方是 **`QaEditor` 的 Q 欄位**（`ImeInput className="kg-input !w-auto flex-1"`，缺 `min-w-0`）——在 iPhone 標準寬度（390px）量不出來，但縮到 320px（iPhone SE 這一檔）就會整張卡片橫向溢出，「刪除」按鈕跑出卡片右邊界約 57px。

**已完成**：`QaEditor` 的 Q、A 兩個欄位（A 欄位跟 Ticket-02 是同一個 bug）都加上 `min-w-0`；順手掃過整個 `kg.tsx` 找到同樣缺 `min-w-0` 的另外兩處（`RelationNotes` 的新增筆記欄位、圖片／影音貼網址欄位），一併修掉。用 Playwright 在 320px 寬重新量測，卡片溢出與按鈕位移都歸零。

#### Ticket-07　轉跳時導覽列文字閃爍與 Logo 顯示錯誤（整合舊 8、新 11）

**症狀**：

- 企劃名稱左側誤顯示為「牽關 Logo」而非企劃自己的 Logo（企劃若有上傳頭像應優先使用頭像）。
- 頁面轉跳時，導覽列會突然變成「網址名字」閃爍一下。在同一個企劃內轉跳時，導覽列狀態應該保持穩定。

**要查**：對應 P2 第三步「導航」裡「頁首左邊的圖示改企劃自己的封面或縮圖」那件事——目前 `project-shell.tsx` 的頁首邏輯還沒做這個切換，這張票是把那件事的實測症狀寫清楚。

### 🟡 UX 體驗與動線優化（UX / UI Improvements）

#### Ticket-08　導覽列與儲存按鈕空間重構（整合舊 5、跑版 Bug 4）

**症狀**：手機版底部導航與儲存列疊加，吃掉約 25% 螢幕空間，且底部體驗不佳。

**期望**：導覽列統一放上方（手機版簡化為 Icon）。儲存按鈕改為右上角「懸浮 Icon」。如果保留儲存列，沒有變更時收合，有變更才滑出。

#### Ticket-09　移除「填寫模式」，統一使用「組版模式」（更新自舊 10）

**決定**：雙模式切換體驗太痛苦，廢除「填寫模式」，全站統一使用「組版模式」。

> 這個決定跟先前一份未定案的外部分析（建議反過來、把組版併進填寫模式）方向相反——最終定案是這一版：只留組版模式，不留填寫模式。動手前確認這點沒有疑義。

**影響範圍**：`BlocksEditor`/`BlockEditCard`（`kg.tsx`）目前有 `mode: 'schema' | 'fill'` 兩條完全分開的渲染路徑（`BlockFieldRow` 給組版、`FillFieldRow` 給填寫），`Manage.tsx`/`CharEdit.tsx` 都有組版／填寫切換 UI 與對應的模式狀態、以及「加完會回到填寫」這類跳轉邏輯。全部要拆掉，只留組版路徑；`BlocksEditor` 的 `mode` prop 與相關切換文案、`onRequestSchema`/`onAdded` 那套「填寫模式加完跳組版再跳回填寫」的邏輯，屆時要一併清理，不要留著沒人呼叫的死路徑。

**跟 Ticket-02 的關係**：組版模式（`BlockFieldRow`）目前對 `textarea` 型別是直接內嵌一個 `rows={4}` 的 `<textarea>`，沒有走會壞掉的 `FillSheet`；併成單一模式後，長文編輯器要不要在組版模式底下也做一個「需要整版才夠用」的全螢幕編輯入口，還是就用組版模式現有的內嵌大文字框，兩種都可以，但要在動手前定一個方向，不要兩條路徑同時留著造成新的分裂。

#### Ticket-10　自有角色免發牽關邀請（新增）

**描述**：如果牽關的雙方角色都是同一個使用者的，系統應自動通過，不用走「送出邀請」流程，直接進入編輯狀態。

**要查**：`services/relation.ts` 的 `initiate()`／`respond()` 目前完全沒有比較兩隻角色的 `discord_id`；要加的判斷是「`from.discordId === to.discordId` 時，`initiate()` 直接寫入 `status: 'accepted'`，不經過 `pending`」。

#### Ticket-11　角色加入企劃的狀態不同步

**描述**：第一次點擊加入角色時不會馬上加入，要「編輯並儲存」才會生效。應改成點擊後直接打 API 加入。

**要查**：這是 P1-3／§12 那個「新建角色 `status='draft'`，首次儲存才轉 `active`」的既有設計，目前故意讓角色卡「先建立、可能不完成也留著草稿」——這張票等於要推翻那個設計，改成加入當下就是最終狀態。動手前要跟主催確認清楚是要拿掉 draft 狀態本身，還是只是「加入」這個動作要更即時地給使用者回饋（例如加入後直接導去角色頁而不是留在原地），因為兩種理解對應到完全不同的修法。

#### Ticket-12　頁面切換的載入過場（Layout Shift）

**描述**：切換頁面（尤其企劃與角色列表）內容會分段彈出。希望能等資料載入完畢後一次顯示，或加入過場動畫。

**要查**：對應既有的 1-2「換頁時內容分批跳出」，`usePathRoute`／`startTransition` 的機制已經在，這張票代表實測下來還沒完全解決，可能是個別頁面（列表頁）自己的請求瀑布還沒拆成並行請求。

#### Ticket-13　Discord 登入未喚醒原生 App

**描述**：手機版點擊 DC 登入時，沒有自動跳轉開啟 Discord App 授權（deep link），導致需在網頁重輸帳密。

**要查**：目前 `login` 路由直接把使用者導去 `https://discord.com/api/oauth2/authorize`，這是網頁版網址；要接 app 喚醒通常要嘗試 `discord://` 自訂 scheme 或依賴 iOS Universal Link／Android App Link，且要處理「裝置沒安裝 App」的 fallback，這塊瀏覽器相容性差異大，動手前先確認優先度（跟 Ticket-01 的 Safari 迴圈比，哪個先修）。

### 🟢 新功能與進階需求（Feature Requests）

#### Ticket-14　單向牽關與互動筆記功能

**描述**：若對方尚未登入企劃，目前只能紀錄名字。希望能開放單向的「紀錄關係與互動筆記」欄位。

**要查**：這跟 1.5-2 的 `private_relations` 表本來就是同一件事——`label`／`note` 欄位已經存在（見前面 1.5-2 的 schema），前端 `Relations.tsx` 的「記下對方名字」表單目前只收 `ghostName`，沒有暴露 `label`／`note` 輸入框。這張票可能只需要補前端表單欄位，不需要動資料庫。

#### Ticket-15　滿版滑動圖集與圖片留言（新增）

**描述**：多張圖片希望能像社群平台一樣「無邊框、滿版左右滑動（swipeable）」，並支援簡單附上留言／說明文字。

**要查**：`GalleryView`（`kg.tsx`）目前的 `layout` 只有 `carousel`／`grid` 兩種，且 `BlockField` 的 `images: string[]` 是純字串陣列，沒有逐張的說明文字欄位——要支援圖片留言，資料結構要從 `string[]` 改成 `{ url: string; caption?: string }[]` 這類，屬於欄位資料格式變更，要處理既有資料的相容（舊資料是純字串陣列）。

#### Ticket-16　企劃專屬 Open Graph 預覽圖（整合舊 6、新 11）

**描述**：企劃網址貼到外部（Discord／噗浪）時，預覽圖應動態抓取主辦方上傳的企劃封面圖，不是網站預設 Logo。

**這張票可能已經部分解決，實測後再決定要不要動**：`app/api/src/index.ts` 的 `servePage()` 已經會用 `ogImageOrNull(p.coverUrl)` 動態帶入企劃封面（P2 第一步 path routing 上線時一起做的）；唯一的已知限制是 `ogImageOrNull()` 只認 `http(s)://` 開頭的網址，`data:` URI 封面（使用者直接上傳、還沒接 R2 圖片管線前的常態）會被視為「沒有圖」退回預設 Logo——如果主催上傳的封面是 `data:` URI，這就是預期中的限制，不是新 bug，要解要等 0-2 提過的 R2 圖片管線接上之後才有真正的圖片網址可用。動手前先確認主催回報的情境是不是這一種。

### ⚪ 暫緩／待處理區（Deferred / Future）

#### Ticket-17　收合功能與 Tab 分頁元件（新增）

**描述**：為了讓長篇幅內容更好閱讀，未來需要新增「折疊收合（accordion）」或「Tab 分頁」的排版元件。列入下階段開發清單。

**備註**：`kg.tsx` 已經有 `FillSection`（手風琴摺疊，`kg-acc-head` 樣式）跟 `RowMenu`（"⋯" 選單）這兩個現成元件，Ticket-09 拆掉填寫模式時，這兩個元件在組版模式裡怎麼重用（例如複雜欄位改成手風琴展開而不是跳全螢幕）可以先想，但排入下一階段執行，不在這一輪動工範圍。

---

## P3 — 細節

| 項目 | 說明 |
|---|---|
| 複製邀請連結 | 主催的核心動作是複製貼進 Discord。按鈕放企劃頁最顯眼處，不要藏在管理後台 |
| 空狀態引導 | 新企劃只有主催一人時，動態牆與名單都空。那一刻要顯示「把連結貼給大家就可以開始了」＋複製按鈕 |
| pending 顯示在等誰 | 列表寫「等 ◯◯ 回應」，並給一個複製提醒訊息的按鈕，讓使用者知道該去催誰 |
| 返回鍵關 modal | 手機（尤其 Android）按返回應該關掉 modal，而不是離開整個頁面 |
| 貼上時的隱形字元 | 從 Discord 複製常帶前後空白或零寬字元。`trim()` 之外要濾掉 `\u200B-\u200D\uFEFF` |
| 無障礙補完 | modal 的 `role="dialog"` / `aria-modal` / Escape / focus trap、表單 `htmlFor` 關聯、skip link |

---

## 施工順序建議

1. ~~P0 三項~~ — 已完成
2. ~~1-1 導航、1-2 換頁、1-3 動態牆、1-4 編輯碼救援~~ — 已完成
3. ~~P1.5（私人關係表與關係第三層）~~ — 已完成
4. ~~P2 第一步（path routing + OG meta）、第二步（Discord 登入取代權杖）~~ — 已完成並部署（2026-09-04）
5. **P2.5 上線後實測回報（Ticket-01～17）** — 見上方，🔴 Critical 四項先修，其餘按 🟠→🟡→🟢 順序；⚪ 暫緩區不排進這一輪。
6. **P2 第三、五步（導航拆分、關係圖）** — 部分內容已併入 P2.5 的 Ticket-07/08，其餘照原順序做。
7. **P3** — 隨手做

> 注意：P2 第二步把權杖系統整個換成 Discord 單一身分，跟先前規劃、已作廢並收進 `docs/archive/` 的「Discord 帳號整合（選配連結）」設計是兩個不同方向——後者假設權杖系統繼續存在、Discord 只是額外的一種找回方式；這裡是 Discord 變成唯一身分，權杖被刪除。兩者不能同時要，這份文件走的是後面這一版。
