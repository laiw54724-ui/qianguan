# 牽關 — 問題整理與工單

> 2026-09-03 ｜ 對象：`qianguan.beibeiz.workers.dev` 已部署版本
> 用法：由上往下做。P0 是現在就在壞的，P1 是結構性的，P2 是新功能，P3 是細節。

---

## P0 — 現在就在壞

### 0-1 加入碼沒有被儲存 — ✅ 已修復（2026-09-05）

~~**症狀**：建立企劃時設了加入碼，之後在設定裡看不到，加入時也驗不過。~~

**實測確認**：這張票描述的原始症狀（草稿／發布雙層模型時代的舊 bug）在 P1.5／P2 那幾次重構時已經順便修好——`project.ts` 的 `createProject()`／`patchProject()` 都是**建立/更新當下就寫入 `join_code_hash`**，不是等發布。用 curl 對本機 dev server 實測：設定加入碼後 `has_join_code` 立刻是 `true`，用同樣格式（含大小寫差異）的碼可以正常加入。

**但實測同時發現驗收標準寫的兩個變體沒有全過**：原本的 `normJoinCode()` 只有 `trim().toLowerCase()`，只處理頭尾空白跟大小寫，**沒有處理加入碼中間的空白**。驗收標準要求碼設成 `Fog 2026`（中間有空白）時，`fog2026`（去掉中間空白）跟 ` FOG2026 `（頭尾空白+去中間空白）都要能通過——這兩個都會在原本的實作下失敗（因為正規化後仍是 `"fog 2026"`，跟 `"fog2026"` 不相等）。

**已修復**：`normJoinCode()`（`app/api/src/auth/token.ts`）改成拿掉**全部**空白（`s.replace(/\s+/g, '').toLowerCase()`），不只是頭尾——加入碼是要在 Discord 上手動轉述、打字的一次性密語，不是結構化欄位，使用者打字習慣的空白差異不該造成加不進去。補了 `project.test.ts` 的回歸測試（建企劃設 `Fog 2026`，分別用 `fog2026`／` FOG2026 ` 加入都要成功，用錯誤密語要失敗），並用 curl 對本機 dev server 重新實測過一次，確認兩種輸入都能加入成功。

### 0-2 `og:image` 是空的 — ✅ 已完成

~~**症狀**：HTML 外殼裡 `<meta property="og:image" content="">`，同時 `twitter:card` 宣告 `summary_large_image`——等於承諾一張大圖卻不給，比不設還糟。~~

已放好 `app/web/public/og-default.png`（1200×630）並填進 `index.html` 外殼的 `og:image`/`twitter:image`。企劃頁與角色頁各自的動態 OG（依角色/企劃內容覆寫）見 P2 第一步。

### 0-3 手機輸入時畫面自動放大

**症狀**：iOS Safari 點進輸入框時整頁縮放。

**原因**：iOS 的固定行為——輸入框字級小於 16px 就自動放大。

**要做**：

```css
input, textarea, select { font-size: 16px; }
```

桌面版要小一點再用 media query 調。**不要用 `maximum-scale=1` 或 `user-scalable=no`**，那會連帶關掉使用者自己縮放的能力，是無障礙倒退。

**驗收**：iPhone Safari 上點任一輸入框，畫面不動。

---

## P1 — 結構性問題

### 1-1 企劃層導航

**問題**：進了 `/p/:slug` 之後，建角色等頁面跳出了企劃的脈絡。在企劃內的所有操作都以企劃為單位，導航必須反映這件事。

**要做**：`/p/:slug/*` 底下共用一個殼。

- 頂部固定：企劃名稱（點擊回企劃首頁）
- 底部固定四格，**圖示 + 兩個字**（純文字在手機上會被截斷）：動態／名單／我的／設定
- 建角色、發起牽線這類動作從頁面內進入，不佔導航格
- 底部列加 `padding-bottom: env(safe-area-inset-bottom)`，否則 iPhone 的 home indicator 會蓋住

### 1-2 換頁時內容分批跳出

**問題**：切頁面時先出現空殼，內容隔一下才陸續出現，還會位移。

**要做**：兩件事一起。

1. 用 React 19 的 `startTransition` 包住路由切換——**舊畫面會留在原地直到新畫面準備好**，這正是「跑完再一起顯示」。用 `useTransition` 的 `isPending` 驅動頂部進度條。
2. 檢查請求瀑布。如果企劃頁是「先抓企劃 → 再抓角色 → 再抓關係」三輪往返，改成一個端點一次回傳，或至少三個請求並行。

**驗收**：手機上連續切換三個分頁，畫面不出現空殼閃爍。

### 1-3 動態牆改為主動發布

**問題**：「◯◯ 更新了設定」是自動追蹤，這個社群有不少人不喜歡被關注。

**決定：不做可見性開關，改掉事件本身。** 開關會變成一個要設定的東西，而且預設值怎麼選都會傷到一群人——預設開，內向的人被暴露；預設關，動態牆是空的。

- **保留**：「◯◯ 加入了」（加入是公開行為）、「◯◯ 和 ◯◯ 牽上線了」（雙方都同意過）
- **移除**：「◯◯ 更新了設定」的自動事件
- **新增**：角色頁存檔後問一句「要不要跟大家說一聲？」，可附一行字（例：「補了背景故事」）。預設不發，不想說就不說。

改完之後動態牆的內容從「有人動了什麼」變成「有人想讓你看什麼」，隱私問題自然消失，資訊量還變高。

### 1-4 編輯碼救援

> （P2 第二步會連同權杖系統一起刪除，這是預期）

**問題**：編輯碼只顯示一次，在手機上很危險——使用者按了複製，接著跳去 Discord 貼連結，剪貼簿就被蓋掉了。這會是最大的客服來源。

**要做**：

- 顯示編輯碼的畫面要勾選「我已經存好了」才能繼續
- 角色頁常駐一個「重看編輯碼」入口，用現有 cookie 驗證即可（人已經在自己的角色頁了，沒有新增風險）

---

## P1.5 — 動工 P2 之前要併的幾件事

> 2026-09-04 新增：這兩項在 P2（尤其 Discord 淨刪除權杖那一步）動工前要先定案，不然關係模型跟單人可用性會被 P2 的重構晚一步撞見。

### 1.5-1 關係卡少了一層：共用互動

實際填寫時的內容有三層，現在的模型只有兩層：

| 層 | 例 | 現況 |
|---|---|---|
| 各自視角 | 「跟著他可以吃他剩下的東西，賺爛了」 | 已有 `a_note` / `b_note` |
| 關係標籤 | 朋友 | 已有 `a_label` / `b_label` |
| 兩人共用的互動 | 「烏鴉喜歡拔狼毛，飛走再回來拔」 | 沒有 |

第三層不屬於任何一方的視角。原型裡「其他補充・雙方共用」就是它，但沒定義清楚。

```sql
CREATE TABLE relation_notes (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  relation_id INTEGER NOT NULL REFERENCES relations(id),
  body        TEXT NOT NULL,
  author_side TEXT NOT NULL,          -- 'a' | 'b'
  created_at  INTEGER NOT NULL
);
CREATE INDEX idx_rnotes ON relation_notes(relation_id, created_at);
```

**規則**：關係 `accepted` 之後才開放（`pending` 期間不能寫）；雙方都能新增，但只有新增者能刪自己那條；不做即時協作、不做編輯歷史。用條列不用自由文字。

### 1.5-2 單人也要能用完，這是優先度問題

這個站原本的構想是給自己整理用的。現在整套設計預設「要有第二個人來、要對方同意、才有東西看」——如果只有三五個人註冊，先來的人會打開一個空的站然後不回來。

所以單人路徑必須完整可用：

- 建角色、記關係、看關係圖，一個人就能全部做完
- 對方不在站上時，關係以純文字記下對方名字，只有自己看得到，不進公開頁、不進動態、不通知任何人
- 雙向同意是加值，不是前提

> **未確認的關係紀錄一律私人。**

**機制定案：不建「幽靈角色」，只記私人筆記——不要讓人幫別人建角色。**

代建是壞主意，理由：

- **佔名**：建了一個「阿楠」，這個名字就在企劃裡了。本人來的時候要嘛認領代建的版本，要嘛變成兩個阿楠。
- **替人發言**：代建的內容是你眼中的他，但掛的是他的角色名。認領之後那些字還在，同人圈對這件事很敏感。
- **重複**：A 和 B 各自幫阿楠建一隻，就有兩隻要合併，而且沒有乾淨的合併方式。
- **名單污染**：二十個人各自幫其他十九個代建，最多 380 筆幽靈列。
- **最麻煩的是持續性的規則負擔**：幽靈角色必須從名單、動態牆、關係圖、OG meta、搜尋全部排除，而且是「每次新增查詢都要記得加這個過濾」的規則，遲早會在某一處漏掉。

最後一點是決定性的：**結構上不可能發生，勝過一條要一直記得套用的規則。**

改成不建角色、只記一筆私人筆記。差別在於它宣稱了什麼——幽靈角色宣稱「這個人的角色存在於這個企劃」，私人筆記只宣稱「我跟一個我叫他阿楠的人有關係」。後者才是事實。

用**獨立的表**，不要塞進 `relations`：

```sql
CREATE TABLE private_relations (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id     TEXT NOT NULL REFERENCES projects(id),
  owner_char_id  TEXT NOT NULL REFERENCES characters(id),  -- 我的角色
  ghost_name     TEXT NOT NULL,        -- 對方的名字，純文字
  label          TEXT NOT NULL DEFAULT '',
  note           TEXT NOT NULL DEFAULT '',
  linked_char_id TEXT REFERENCES characters(id),  -- 日後對上真人時填
  created_at     INTEGER NOT NULL,
  updated_at     INTEGER NOT NULL
);
CREATE INDEX idx_priv ON private_relations(owner_char_id);
```

分開一張表有兩個實際好處：

1. `relations` 靠 `a_id < b_id` 正規化，塞一個可為空的 `b_id` 進去會直接破壞唯一索引。
2. **凡是查 `relations` 的地方，天生就是公開安全的。** 不需要記得過濾。

**轉正的流程**：對方真的加入之後，在他的角色頁或你的關係頁顯示一句：「你有 2 筆私人紀錄寫著『阿楠』，站上現在有同名角色，要送出正式邀請嗎？」按下去才轉成一筆 `pending` 的正式關係，走既有的雙方確認。名字比對只做建議，**絕不自動轉換**，也不通知對方——在他同意之前，他不該知道有人記了他什麼。

**主催能不能預先建名單？不要做。** 看起來方便，但它把整個所有權問題原封不動搬回來。

### 1.5-3 淨刪除：`draft-char`（代建角色）整條流程

跟 1.5-2 是同一個決定的另一半：既然不讓人代建角色，`draft-char` 這條路就不該存在。

**要刪掉的東西**：

- `POST /api/p/:slug/c/:charId/draft-char` 端點
- `Relations.tsx` 裡「請填對方角色的名字」那段建角色流程與對應前端狀態
- 換成：寫進 1.5-2 的 `private_relations`

這也順便解掉「無主角色」的問題——**規則變成：角色只能由本人建立，沒有例外。** P2 第二步 Discord 換掉權杖時，就不會有「這隻角色沒有 `discord_id` 該歸誰管」這種要特別處理的情況。

又是一次淨刪除，跟 P2 的方向一致。

---

## P2 — 分享優先重構

> 2026-09-04 更新：這一節取代舊版「角色分享與跨企劃牽關」——先解決一個矛盾，再排出施工順序。

### 先解一個矛盾：登入牆 vs 分享

「不開放訪客」和「角色名片能分享到噗浪」直接打架——分享出去的連結若撞上登入牆，分享就沒意義。

**規則：讀取公開，寫入登入。**

- 任何人都能看角色卡、企劃卡（unlisted 企劃除外）
- 建角色、發起牽關、編輯、加入企劃，一律要 Discord 登入

要防的是匿名亂發牽關，不是防人看。

### 第一步：hash routing 換成 path routing

優先度最高，因為它擋住「能分享名片」這個核心目標。

現在網址是 `/#/p/xxx/c/yyy`。井字號後面的東西永遠不會送到伺服器——Worker 看不到是哪個企劃或角色，所以 OG meta 寫不出來，分享出去永遠是同一張通用卡。這不是樣式問題，是路由架構問題。

**要做**：

- 前端路由改用 History API（真實路徑 `/p/xxx/c/yyy`）——核心引擎在 `app/web/src/lib/nav.ts`（`navigate`/`href`/`useHashPath`），連帶 `dirty.ts:111` 的離開攔截導覽、`App.tsx` 那個因為 hash 路由才需要的 `#main` skip-link workaround，都要跟著換掉，不是只改路由表
- Worker 對所有非 `/api/*` 的路徑回傳 SPA shell
- 接上規格 §11 的 HTMLRewriter，注入企劃／角色的 OG meta（`og:image` 預設值 0-2 已經修好，這裡只是沿用既有機制，不用重做）
- **拿掉兩處寫死的 hash 轉址，不是改字串**：`app/api/src/index.ts` 第 394、414 行，既有 §11 OG meta 邏輯（`servePage()`）裡有 `<meta http-equiv="refresh">` 把瀏覽器彈去 `` `/#/p/${slug}${charId ? `/c/${charId}` : ''}` ``——這是 hash 路由時代的補丁，因為爬蟲抓得到靜態 meta 但瀏覽器要另外導去 JS 接手的位置。**path routing 之後這個轉址整個不需要**：JS 會在同一個網址原地接手，不用再彈去別的地方。把 `HeadRewriter` 建構子傳進去的 `redirect` 參數改成 `null`，不是把字串裡的 `/#/` 換成 `/`。（掃過 `docs/archive/` 那份作廢計畫裡 OAuth callback 的 hash 轉址：那段程式碼從沒進過 main，不用管。）
- **加一段 hash → path 的相容重導**（十行內）：Worker 收到帶 `#` 的舊連結時（實務上偵測不到 fragment，這段其實是給還留著 `/#/p/xxx` 書籤或轉貼連結的使用者，用一小段前端 boot 腳本讀 `location.hash` 存在就 `history.replaceState` 轉成新路徑）轉到新的 `/p/xxx` 路徑。現在只有測試資料，成本低，之後路由已經散出去再補會麻煩很多。

**隱私規則（規格 §11）**：unlisted 企劃只給通用 meta，不得輸出真實標題與封面。角色卡即使在 unlisted 企劃底下也要能分享，但 OG 只帶角色本身的資訊，不帶企劃名稱。

**驗收**：把角色連結貼進 Discord 與噗浪，展開出角色名與頭像的卡片；手動打開一個舊的 `/#/p/xxx` 連結，確認會被轉到新路徑而不是 404。

### 第二步：Discord 登入取代權杖系統（淨刪除）

角色卡現在是跨企劃的身分、別人可以對你發起牽關——這已經是帳號形狀的產品，「不用註冊」的前提不成立了。

**要刪掉的東西（這一步做完程式碼會變少）**：

- 編輯碼 `chr_` / 開設者碼 `own_` 的產生、顯示、貼碼救援 UI
- 權杖三層存放設計、TokenGate 元件
- `user_links` 的 AES-GCM 加密與 `LINK_KEY`
- `confirmed` 欄位與待確認清單
- `kg_u` 的 HMAC 簽章
- 共用電腦誤連結的整套防護

上述全部隨著「Discord 是唯一身分來源」一起消失。Discord 整合設計文件裡的加密、`confirmed`、freshness 討論全部作廢——那些是為了「Discord 只是選配」而存在的複雜度。

**新的模型**：

```
sessions: discord_id 是唯一身分
characters.discord_id  → 誰擁有這隻角色
projects.owner_discord_id → 誰開的企劃
```

這兩個欄位早就預留在 schema 裡，現在真的派上用場。

**保留**：state 一次性存 KV、回呼轉址目標只從 state 組出（不接受查詢參數）、scope 只要 `identify`、不存 username／avatar。

同一個 Discord 帳號可以有多隻角色，`discord_id` 不要加 unique 約束。

### 第三步：導航

桌機與手機要分開。底部標籤列是手機模式，桌機用左側或頂部導航並加寬內容區。

頁首左邊的圖示改成企劃自己的封面或縮圖，沒設就用企劃名第一個字做字母標。牽關的 logo 只留在站首頁。

底部列保留 `padding-bottom: env(safe-area-inset-bottom)`。

### 第四步：首次繪製與跑版

#### 4-1 畫面在拿到資料前就先畫了

**症狀**：點編輯或設定時會閃一下「請輸入權杖」再跳走；切換企劃／角色列表時內容分批跳出。

這兩個是同一個病——畫面在還不知道自己是誰、還沒拿到資料時就先繪製。

**要做**：把首次繪製擋在一個 bootstrap 查詢後面，session 與該頁資料都回來之前只顯示骨架，不要先畫出一個馬上會被抽掉的狀態。搭配 `startTransition` 讓舊畫面留到新畫面備妥。

兩個要一起修，不要分開處理。

#### 4-2 固定頁首蓋住內容

捲動後子編輯器的標題與「完成」按鈕被企劃頁首壓住。內容容器加 `padding-top` 等於頁首高度，錨點目標加 `scroll-margin-top`；或子編輯器改成疊在頁首之上的 sheet，不共用同一條捲動流。

#### 4-3 輸入框溢出卡片（關係卡、Q&A 都是同一個毛病）

「區塊標題」「Q」的輸入框比卡片寬，把「刪除」擠到卡片外。那一列缺 `min-width: 0`——flex 子元素預設不會縮到內容寬度以下。

#### 4-4 長文編輯器是空的且不能捲動

點「點擊寫長文」跳進去只有一個白框，內容區沒有高度也沒有捲動。Q&A 的 A 同一個元件同一個症狀。這個是壞的，不是體驗問題。

**修法**：內容區給明確高度、可捲動、鍵盤彈出時輸入區吸附在鍵盤上方、「完成」正確寫回原欄位。

#### 4-5 兩條固定列吃掉螢幕

儲存列加底部導航疊在一起，小螢幕上約 25% 是介面外框。沒有未儲存變更時把儲存列收起來，只在有變更時滑出。

### 第五步：關係圖

先講實話：二十個角色的全域關係圖在 380px 螢幕上不可能好用。它的價值不在使用，在截圖分享——主催會把它貼到群裡。

所以定位成展示品，不是操作介面：

- 桌機：完整力導向圖
- 手機：預設「以我為中心」的一層放射圖 + 名單，超過 8 個退回列表
- 提供一鍵匯出成圖片
- 不要在手機上做拖曳畫布

### P2 施工順序

1. ~~path routing + OG meta（解鎖分享）~~ — 已完成並部署
2. ~~Discord 登入取代權杖（淨刪除）~~ — 已完成並部署（2026-09-04）
3. 導航拆桌機／手機、logo 換企劃的 — 見下方 Ticket-07/08
4. bootstrap 擋首次繪製 + 五項跑版 — 見下方 Ticket-02/05/06/08，內容已被實測回報取代，本節保留供對照
5. 關係圖

**不要重寫整個專案。依序改，每一步結束都要能跑。**

---

## P2.5 — 上線後實測回報（Ticket-01～17）

> 2026-09-05 新增：P2 第一、二步部署後，主催實測回報的問題與新需求，取代上面「第四步：首次繪製與跑版」的推測版本（4-2/4-3/4-4/4-5 對應到下面 Ticket-05/06/02/08，內容更新為實測結果）。用法沿用文件開頭：由上往下做，🔴 是現在真的壞的，🟠 是跑版，🟡 是體驗，🟢 是新功能，⚪ 先不做。

### 🔴 優先修復：核心 Bug 與功能異常（Critical）

#### Ticket-01　Safari Discord 登入跳針無限迴圈 — ✅ 已修復（2026-09-05）

~~**症狀**：在 Safari 用 Discord 登入後，狀態沒有正確保留，畫面一直跳回要求授權。~~

**實測確認的根因，跟原本猜的方向都不一樣，也跟 Safari 本身無關**：用 `wrangler tail` 接上正式環境即時log，請主催在真的 Safari（不是 Discord App 內建瀏覽器）上實際跳一次登入重現，結果很清楚：**Discord OAuth 流程本身每一次都完全成功**——state 讀到、code 換到 token、拿到 `discord_id`、session 建立、`Set-Cookie` 都正常送出，沒有一次失敗。

真正的迴圈來源：首頁「我的角色」清單（`Poster.tsx`，靠瀏覽器 `localStorage` 記住的角色，見 `lib/session.ts`）會抓其中一筆角色的關係資料，而這筆角色**不是目前登入的這個 Discord 帳號的**（很可能是 Discord 遷移那次正式環境資料整個清空之前留下的舊紀錄）。這個請求正確地收到 401，但前端的全域 401 攔截器（`api.ts`）看到任何 401 都直接導去 Discord 登入——即使使用者已經登入了，即使 `Poster.tsx` 自己原本就用 `.catch(() => [])` 想要安靜吞掉這種失敗。因為「用同一個帳號再登入一次」永遠不會讓你突然擁有那隻角色，這個迴圈會永遠重複下去。

**根本問題**：後端對「根本沒登入」跟「登入了但這個資源不是你的」回的是同一個 401（語意上應該分 401／403，但要動到所有權限檢查的路由，範圍較大，先不做）。**已修好**：前端在導去登入頁之前，先問一次 `/api/me` 目前是否真的有 session；只有真的沒登入才導頁，已登入的話代表只是這個資源的權限問題，讓原本呼叫端的錯誤處理（例如 `Poster.tsx` 的 `.catch(() => [])`）正常運作，不強制跳頁。改的是 `app/web/src/lib/api.ts`，後端沒有動。

**同一次稽核順手修掉、但確認跟這次症狀無關的加固**：`redirect_uri` 原本在 `/login`／`/callback` 兩個獨立請求裡各自重算一次，Discord OAuth2 規定兩邊要逐字元相同，理論上有風險（雖然這次 log 顯示它其實一直是對的）——已經改成存進 KV state，兩邊讀同一個值。

**驗收**：主催已在真實 Safari 上重新測試登入，確認不再跳針。

#### Ticket-02　長文編輯器與 Q&A 空白死機（原跑版 Bug 3）— ✅ 已修復（2026-09-05）

~~**症狀**：點擊「寫長文」或 Q&A 的 A，跳出的編輯器只剩白框，沒有高度也無法捲動。這屬於功能損壞，需優先修復。~~

**實測確認的根因**：用 Playwright 開角色編輯頁重現後發現，長文的全螢幕編輯器（`FillSheet`）**完全沒有蓋滿螢幕**——外層企劃頁首跟底部導覽都還露在外面，`FillSheet` 自己的標題列跟「完成」按鈕整個不見，因為它被吃進了頁面正常捲動流裡，不是真的 `position: fixed` 蓋滿畫面。

追到的真正原因：`.kg-rise`（幾乎每個頁面區塊都掛的進場動畫）用 `animation-fill-mode: both`，動畫播完後會**永遠**定住 `to` 關鍵影格算出來的 `transform`。就算 `to` 寫的是 `translateY(0)`（視覺上等於沒有位移），瀏覽器實際定住的值是內插算出來的單位矩陣 `matrix(1,0,0,1,0,0)`，不是字面上的 `none`——而只要 `transform` 不是 `none`，這個元素就會變成子孫 `position:fixed` 元素的新包含區塊。`FillSheet` 只要巢狀在任何一個播過 `.kg-rise` 動畫的容器底下（幾乎所有頁面區塊都是），就會被困住，變成卡在頁面裡的一個普通區塊，而不是真正蓋滿螢幕的彈窗。

Q&A 的 A 欄位是另一個獨立的病，跟 Ticket-06 同源：`QaEditor` 的 A 欄位（`ImeTextarea className="kg-textarea flex-1"`）沒有 `min-w-0`，在窄螢幕（實測 320px 寬）被相鄰元素擠壓到視覺上像一格空白。

**已完成**：`.kg-rise` 動畫的 fill-mode 從 `both` 拿掉 `forwards`（改成只留 `backwards`——部分呼叫端有帶 `animationDelay`，這個還是需要），`QaEditor` 的 A 欄位加上 `min-w-0`。兩處都用 Playwright 重現、修好、再重新量測確認過（sheet 蓋滿視窗、textarea 可捲動、「完成」正確寫回內容）。

#### Ticket-03　影音連結（YouTube／Spotify）嵌入失效（新增）— ✅ 已修復（2026-09-05）

~~**症狀**：貼上 YouTube 或 Spotify 連結後無法正常播放，可能 iframe 被擋或嵌入解析邏輯出錯。~~

**實測確認的根因，YouTube 那半是真的壞了，而且是一個很容易再犯的陷阱**：CSP 的設定其實有**兩份獨立的檔案**——`app/api/src/middleware/security.ts`（Worker 自己組的回應）跟 `app/web/public/_headers`（Cloudflare Static Assets 專用的標頭檔）。拿掉 Turnstile 那次（P2 第二步）只改了前者，`_headers` 那份沒跟著改，一直卡著 Turnstile 時代的 `frame-src: challenges.cloudflare.com`。因為大多數頁面（`/home`、`/dashboard`、`/p/:slug/manage` 等——只要不是 `/api/*` 或 `/p/:slug` 的 OG meta 路由）是靜態資產層直接送出，**根本不會經過 Worker 的 `securityHeaders` middleware**，瀏覽器實際套用的 CSP 是 `_headers` 那份，所以 `videoEmbedUrl()` 產生出來的 YouTube iframe 網址完全正確，卻被瀏覽器自己的 CSP 擋下來，用 Playwright 開出來才在 console 看到真正的錯誤：「Framing 'https://www.youtube.com/' violates ... frame-src https://challenges.cloudflare.com」。兩份檔案現在都改成一致，且互相加了「改一邊要記得改另一邊」的註解提醒。

Spotify 確認過就是原本說的「從沒做過」，已經決定要做：新增 `spotifyEmbedUrl()`（解析 `open.spotify.com/{track|album|playlist|episode|show}/<id>`，轉成 `/embed/` 版本、拿掉 `?si=` 追蹤碼），接到 `MediaView` 的音樂欄位分支，CSP 的 `frame-src` 兩份都加了 `open.spotify.com`。用 Playwright 實測 YouTube 影片與 Spotify 單曲各嵌一次，畫面正常顯示（YouTube 縮圖／播放鍵、Spotify 專輯封面／播放控制都正常渲染），console 零錯誤。

#### Ticket-04　儲存狀態誤判與效能卡頓 — 🔶 一半修好，一半是已知較大工程（2026-09-05）

~~**症狀**：編輯後按儲存要等很久；儲存完退出時系統仍誤判成「還沒存」，跳出重複的離開警告。~~

**「誤判」那半，已修復**：原本猜的方向（`dirty` 比對、`snapshot.current` 忘記更新）追下去發現是錯的——實際測試多種情境（純文字欄位、QA 只填一半會被存檔時濾掉的項目）`dirty` 都正確在存檔後立刻變 `false`，這條假設不成立。真正的問題在 `Manage.tsx` 的 `doSave()` 有一行 `if (saving) return false;`——如果存檔還在進行中（例如按了「儲存」還沒回來）又被叫一次（最典型：使用者馬上點連結想離開，跳出「有未儲存的變更」，按下「儲存並離開」），這行會**直接假裝失敗回傳 `false`**，而 `useLeaveGuard` 把 `false` 當成「存檔失敗」處理，讓使用者卡在「明明按過儲存」卻又被說沒存好的狀態——即使原本那次存檔幾秒後其實會成功。`CharEdit.tsx` 更嚴重，完全沒有這層防護，會真的送出兩個並發的 PATCH 請求。兩處都改成用同一個進行中的 Promise 分享給後來的呼叫端，大家等的都是同一次真正的存檔結果，不會有人被假訊息誤導。

**「效能卡頓」那半，找到根因但不是這次工程範圍**：專案的圖片還沒接上 R2（0-2 已經記過這個缺口），封面／頭像／相簿圖片都是直接把整張圖轉成 base64 塞進 `world_blocks`／`cover_url`／`icon_url` 這些 JSON 欄位裡存進 D1。問題是**每一次按「儲存」都會把目前整包狀態原封不動重送一次**，包含所有沒有改變過的圖片——如果一個角色卡已經存了封面、頭像、幾張相簿圖，之後只是改一個文字欄位再按存檔，那幾張沒變過的圖片還是會整包重新上傳一次。連線越慢、圖越多，這個「儲存要等很久」的體感就越明顯，而且會隨著使用時間越用越慢（累積的圖片只多不少）。**這不是一個小 bug，修法是把圖片管線接上 R2（存網址不存整張 base64），屬於 0-2 已經記錄、範圍更大的既有工單，這次先把根因記清楚，不在這次動工。**

### 🟠 介面跑版與顯示錯誤（Layout / UI Bugs）

#### Ticket-05　固定頁首遮擋內容（原跑版 Bug 1）— ✅ 已修復（2026-09-05）

~~**症狀**：捲動後，頁首會蓋住編輯列、大事記標題與子編輯器頂端（吃掉一整行）。~~

**根因**：`ProjectShell` 的頁首是 `position: sticky`，在正常文件流裡仍佔一份高度（58px＝`h-14` 56px＋`border-b-2` 2px）。但瀏覽器原生的「捲到某個元素」行為——手機鍵盤彈出讓可視區域重排、或網址帶 `#hash` 錨點跳轉——只看文件流位置，不知道頁首視覺上疊在最上面那一段。掃過整個 `kg.tsx`／`Manage.tsx`／`CharEdit.tsx` 確認過沒有任何 `scrollIntoView` 呼叫，也沒有第二層 sticky 元素跟頁首搶位置；問題單純是「原生捲動」跟「視覺遮擋」這兩件事互相不知道對方存在。

**已完成**：`ProjectShell` 掛載期間，在 `document.documentElement` 套用 `scroll-padding-top: 58px`（新增 `SHELL_HEADER_HEIGHT` 常數於 `kg.tsx`），離開時還原。這讓瀏覽器原生的捲動—無論是 focus 一個欄位、鍵盤彈出重排、或 `#hash` 跳轉——自動多留頁首的高度，不用逐一幫每個可能的錨點加 `scroll-margin-top`。

**驗證方式**：用 Playwright 對同一個測試錨點（角色編輯頁深處的一個欄位）做 `#hash` 導覽量測——修復前錨點捲到 `top: 88px`（跟頁首的 `bottom: 58px` 有重疊風險，且明顯不是刻意設計的間距），修復後同一個錨點捲到 `top: 146px`，差距剛好等於頁首高度 58px，證實補償生效。**誠實說明**：主催實際回報的情境（很可能是手機鍵盤彈出時的自動捲動）在這個環境只有 Chromium、沒有 WebKit，且 headless 模式不會真的彈出虛擬鍵盤，無法逐畫素重現（跟 Ticket-01 遇到的限制一樣）；這裡驗證的是同一個結構性根因（sticky 頁首不被原生捲動列入計算）的最接近可重現替代方案（hash 錨點跳轉），修法對兩種觸發情境都同樣有效。建議之後在真實手機上再確認一次編輯流程不再出現遮擋。

#### Ticket-06　區塊標題輸入框溢出（原跑版 Bug 2）— ✅ 已修復（2026-09-05）

~~**症狀**：區塊標題輸入框比卡片寬，把「刪除」按鈕擠到卡片外。~~

**實測確認的位置跟 diagnosis 原本猜的不一樣**：世界觀區塊的標題輸入框（`BlockEditCard` 組版模式的標題列）**本來就有 `min-w-0`**，不是它溢出。實際重現出來、且有一個直接可見「刪除」按鈕會被擠出卡片外的地方是 **`QaEditor` 的 Q 欄位**（`ImeInput className="kg-input !w-auto flex-1"`，缺 `min-w-0`）——在 iPhone 標準寬度（390px）量不出來，但縮到 320px（iPhone SE 這一檔）就會整張卡片橫向溢出，「刪除」按鈕跑出卡片右邊界約 57px。

**已完成**：`QaEditor` 的 Q、A 兩個欄位（A 欄位跟 Ticket-02 是同一個 bug）都加上 `min-w-0`；順手掃過整個 `kg.tsx` 找到同樣缺 `min-w-0` 的另外兩處（`RelationNotes` 的新增筆記欄位、圖片／影音貼網址欄位），一併修掉。用 Playwright 在 320px 寬重新量測，卡片溢出與按鈕位移都歸零。

#### Ticket-07　轉跳時導覽列文字閃爍與 Logo 顯示錯誤（整合舊 8、新 11）— ✅ 已修復（2026-09-05）

~~**症狀**：~~

~~- 企劃名稱左側誤顯示為「牽關 Logo」而非企劃自己的 Logo（企劃若有上傳頭像應優先使用頭像）。~~
~~- 頁面轉跳時，導覽列會突然變成「網址名字」閃爍一下。在同一個企劃內轉跳時，導覽列狀態應該保持穩定。~~

**根因一（Logo 顯示錯誤）**：`project-shell.tsx` 的頁首寫死 `<img src="/logo-mark.svg">`（牽關站徽），完全沒有讀取企劃自己的 `icon_url`——不管企劃有沒有設頭像，`/p/:slug/*` 底下永遠只顯示牽關站徽。

**根因二（閃爍）**：用到 `ProjectShell` 的 8 個頁面（`Character`／`CharEdit`／`Join`／`Manage`／`Mine`／`Project`／`Roster`／`Relations`）各自獨立呼叫 `getProject(slug)`，在自己那次請求回來之前一律傳 `title=""`。即使是同企劃內的分頁切換（例如動態→名單），上一頁明明才剛拿到同一份企劃資料，下一頁的元件是全新掛載、状態從頭來過，回來之前 `ProjectShell` 只能落回寫死的 `'牽關'` 字串——這就是「轉跳時閃回網址名字（其實是牽關站名）」的來源，不是特定某一頁的 bug，而是每一次企劃內分頁切換都會發生。

**已完成**：`ProjectShell` 新增 `iconUrl` prop，並用一個以 slug 為 key 的模組層級快取（`shellCache`）記住上一次拿到的 `{title, iconUrl}`；`title` 是空字串（頁面正在載入）時改顯示快取值，而不是落回站名。沒有 `icon_url` 時顯示企劃名第一個字的字母標（圓形色塊），符合 P2 第三步已定案的規格「頁首左邊的圖示改成企劃自己的封面或縮圖，沒設就用企劃名第一個字做字母標，牽關的 logo 只留在站首頁」。8 個頁面都補上 `iconUrl={project.icon_url}`（或 `data.project.icon_url`）。

**驗證方式**：Playwright 實測——先給測試企劃設一個 `icon_url`，確認頁首正確顯示該圖片而非站徽；接著攔截名單頁自己的 `getProject` 請求延遲 1.5 秒模擬慢速網路，點擊「動態→名單」分頁切換後在請求還沒回來的當下讀頁首內容：修復前頁首落回「牽關」字樣＋字母標「牽」，修復後頁首全程維持顯示企劃真正的標題「T34競速測試」與剛剛設定的圖示，沒有中途閃回站名。順手跑過 `tsc -b`、`eslint`、`app/api` 的 `typecheck`／`vitest`（35 個測試全過），確認沒有引入新的型別或 lint 錯誤（跟修改前的 25 個既有 lint 問題比對過，數量一致，都是這次改動之外的檔案）。

### 🟡 UX 體驗與動線優化（UX / UI Improvements）

#### Ticket-08　導覽列與儲存按鈕空間重構（整合舊 5、跑版 Bug 4）— 🔶 最小修法已完成（2026-09-05）

~~**症狀**：手機版底部導航與儲存列疊加，吃掉約 25% 螢幕空間，且底部體驗不佳。~~

**根因**：`StickySaveBar`（`kg.tsx`）在 `CharEdit.tsx`／`Manage.tsx` 都是無條件掛載，不管有沒有未儲存變更都常駐佔用底部空間，跟 `ProjectShell` 的底部四格導覽疊在一起。

**已完成（最小修法）**：`StickySaveBar` 加了 `hidden = !dirty && !busy` 判斷，沒有變更時用 `translateY(100%)` 滑出畫面（不是直接 unmount，避免打字時裝上拆下地閃爍）；同時把 `hidden` 時的 `bottom` 強制歸零——不然 `inShell` 模式原本墊高的 `SHELL_NAV_HEIGHT` 會讓滑出的距離不夠，卡在剛好跟底部導覽同高的位置沒有真正離開畫面（實測時抓到這個問題並修正）。用 Playwright 驗證：全新角色（無未儲存變更）進編輯頁儲存列完全收起、底部導覽完整可見；打字後儲存列正確滑回來。

**保留給之後**：導覽列統一移到上方、手機版簡化為 Icon、儲存按鈕改右上角懸浮 Icon——這是更大的版面重新設計，會影響所有 8 個用到 `ProjectShell` 的頁面的視覺，這次先不做，維持底部導覽現狀。

#### Ticket-09　移除「填寫模式」，統一使用「組版模式」（更新自舊 10）— ✅ 已修復（2026-09-05，選擇方案 B）

~~**決定**：雙模式切換體驗太痛苦，廢除「填寫模式」，全站統一使用「組版模式」。~~

**已完成**：`kg.tsx` 的 `FillFieldRow` 整個刪掉，把它的「預覽→點擊展開全螢幕」互動併進 `BlockFieldRow`——但只套用在 `textarea` 型別（主催選方案 B：長文欄位在組版模式下也用全螢幕 `FillSheet` 編輯，不是內嵌 `rows=4` 的小框，這正是 Ticket-02 死機的舞台）；其餘型別（`text`／`image`／`pdf`／`timeline`／`calendar`…）維持組版模式原本就有的內嵌編輯，沒有改變行為，範圍沒有擴大到沒討論過的型別。`BlockEditCard` 拿掉 `mode==='fill'` 的手風琴分支與 `open`/`onToggle` props（未來要不要在組版模式重用手風琴收合是 Ticket-17 的範圍，這次先維持組版模式原本「全部展開」的行為）。`BlocksEditor` 拿掉 `mode`／`onRequestSchema`／`onAdded`／`seedOpenId` 整組 API，`export type BlockEditorMode` 一併刪除。

`Manage.tsx` 拿掉 `worldMode` 狀態與「填寫／組版」切換 UI，世界觀分頁直接顯示模板選單。`CharEdit.tsx` 影響最大——原本「填寫模式」才有的大頭照／名稱／一句話介紹／分類／連結／企劃欄位（`FillSection` 手風琴）這些 UI，之前完全不會出現在組版模式底下；拆掉雙模式後，這些內容跟角色卡的 `BlocksEditor` 合併成同一個連續頁面，一次顯示所有可編輯的東西，不再需要切換分頁。`wasDraft` 那個「首次存檔（等於 Ticket-11 拆掉前的 draft→active）不彈分享提示」的特殊判斷也拿掉——現在「加入」跟「存檔」是兩個獨立時刻，每次存檔都問一次「要不要跟大家說一聲？」。

**驗證**：Playwright 對 `CharEdit`／`Manage` 兩個編輯頁實測——確認沒有任何模式切換 UI；新增區塊立刻可見、可編輯，不用跳轉；新增「長文」欄位型別的區塊，點擊「點擊寫長文」正確開出全螢幕 `FillSheet`（沒有頁首/導覽穿幫，Ticket-02 的修復沒有回歸），輸入內容、按「完成」、按「儲存」後直接查 D1 確認內容正確寫入。`app/api` 41 個測試全過；前端 `tsc -b` 乾淨，`eslint` 問題數從 25 降到 23（拆掉的死路徑順便修掉兩個既有的 fast-refresh 警告，沒有新增問題）。

#### Ticket-10　自有角色免發牽關邀請（新增）— ✅ 已修復（2026-09-05）

~~**描述**：如果牽關的雙方角色都是同一個使用者的，系統應自動通過，不用走「送出邀請」流程，直接進入編輯狀態。~~

**已完成**：`app/api/src/services/relation.ts` 的 `initiate()` 加了 `sameOwner = !!from.discordId && from.discordId === to.discordId` 判斷；成立時新建/重新邀請都直接寫 `status: 'accepted'`，並比照 `respond()` accept 分支的做法用 `db.batch()` 補一筆 `relation_accepted` 動態（不然動態牆會漏掉這筆牽成紀錄）。先寫了 TDD 回歸測試（`relation.test.ts`）：同一 discord_id 兩隻角色互相發起要直接 accepted 且動態牆有紀錄；不同 discord_id 的既有流程（pending／已牽線判斷）不受影響。`npm run test` 全部 38 個測試通過。

#### Ticket-11　角色加入企劃的狀態不同步 — ✅ 已修復（2026-09-05，選擇方案 B）

~~**描述**：第一次點擊加入角色時不會馬上加入，要「編輯並儲存」才會生效。應改成點擊後直接打 API 加入。~~

**決定**：主催選擇方案 B——拿掉 draft 狀態本身，加入當下就是最終狀態。

**已完成**：`app/api/src/services/character.ts` 的 `joinProject()` 改成 `status: 'active'`，並用 `db.batch()` 在同一交易裡立刻寫入 `char_joined` 動態（不用再等 `patchChar()` 的「首次儲存」分支）。`patchChar()` 原本的 `draft→active` 轉換邏輯保留，但只作為任何舊資料的相容路徑，新建角色不會再走到那個分支。前端 `CharEdit.tsx` 拿掉 `wasDraft` 特殊判斷——「加入」跟「存檔」現在是兩個獨立時刻，每次存檔都問一次「要不要跟大家說一聲？」。

**影響檢查**（動手前逐一查過）：
- `index.ts` 的 draft 角色隱藏邏輯（`GET /p/:slug/c/:charId`）、前端各處 `status === 'draft'` 的「草稿・尚未公開」標籤——全部保留不動，作為任何舊資料的相容路徑，新建角色不會再觸發，不需要資料遷移。
- `listChars()`（公開名單）本來就只回 `active`，現在角色一加入就滿足這個條件，直接出現在名單，不用再等存檔。
- `Join.tsx` 的「已加入」文案本來就沒有依賴 draft 狀態，現在變得更準確（真的已經加入、公開了），不用改。

**驗證**：Playwright 實測完整流程——加入後直接查 D1 確認 `status='active'` 且 `char_joined` 事件已寫入；角色頁沒有「草稿」標籤；`GET /api/p/:slug/chars`（公開名單）立刻包含新角色；編輯頁沒有「這是一張還沒完成的角色卡」提示；存檔後正確跳出「要不要跟大家說一聲？」。`app/api` 補了 3 個回歸測試（加入當下 status/動態/公開名單），`npm run test` 41 個測試全過，前端 `eslint`／`tsc` 跟修改前基準一致（25 個既有問題，沒有新增）。

#### Ticket-12　頁面切換的載入過場（Layout Shift）— ✅ 已修復（2026-09-05）

~~**描述**：切換頁面（尤其企劃與角色列表）內容會分段彈出。希望能等資料載入完畢後一次顯示，或加入過場動畫。~~

**實測確認**：對照了全部 8 個用到 `ProjectShell` 的頁面，`1-2`／`startTransition` 機制確實都在跑，而且 `Project`／`Roster`／`Character`／`Relations`／`Manage`／`CharEdit` 全部**已經用 `Promise.all()` 並行**，沒有誤用的請求瀑布。唯一還有殘留問題的是 `Mine.tsx`——先 `getProject` 拿到 `myCharIds` 才能查角色，這是結構性的兩段式依賴（沒辦法單純拆並行），但它的 `mine` state 用同一個 `[]` 代表「還沒查完」跟「查完是空的」，導致第二段查詢還沒回來前會先閃一下「這個瀏覽器還沒有你在這個企劃的角色」的空狀態提示，查完才變成真正的列表。

**已完成**：`app/web/src/pages/Mine.tsx` 的 `mine` state 改成 `Character[] | undefined`，`undefined`（查詢中）顯示 `PageLoading`骨架，只有查完確定是空陣列才顯示 `EmptyNote`。用 Playwright 攔截該頁的 `getCharacter` 請求模擬卡住的網路，確認修復前會先閃出誤導的空狀態、修復後正確顯示載入中骨架。其餘頁面沒有找到需要再修的請求瀑布，如果之後還有具體頁面回報這個症狀，會是另一個獨立案例。

#### Ticket-13　Discord 登入未喚醒原生 App

**描述**：手機版點擊 DC 登入時，沒有自動跳轉開啟 Discord App 授權（deep link），導致需在網頁重輸帳密。

**要查**：目前 `login` 路由直接把使用者導去 `https://discord.com/api/oauth2/authorize`，這是網頁版網址；要接 app 喚醒通常要嘗試 `discord://` 自訂 scheme 或依賴 iOS Universal Link／Android App Link，且要處理「裝置沒安裝 App」的 fallback，這塊瀏覽器相容性差異大，動手前先確認優先度（跟 Ticket-01 的 Safari 迴圈比，哪個先修）。

### 🟢 新功能與進階需求（Feature Requests）

#### Ticket-14　單向牽關與互動筆記功能 — ✅ 已修復（2026-09-05）

~~**描述**：若對方尚未登入企劃，目前只能紀錄名字。希望能開放單向的「紀錄關係與互動筆記」欄位。~~

**確認**：如預期，後端整套（`label`／`note` 欄位、`privateRelationCreateSchema`、`privRelSvc.create()`、甚至事後編輯用的 `PATCH .../private-relations/:id` 端點）早就做好了，缺口純粹在前端——`Relations.tsx` 建立表單只收 `ghostName`，`label`／`note` 兩個參數寫死空字串；而且 `lib/api.ts` 完全沒有包裝那個既有的 PATCH 端點，就算補了建立欄位，使用者事後也沒辦法編輯已經記錄的內容。

**已完成**：`app/web/src/lib/api.ts` 新增 `updatePrivateRelation()` 包裝既有 PATCH 端點（後端不用改）。`app/web/src/pages/Relations.tsx` 的「記下對方名字」表單加了「你們的關係」「互動筆記」兩個輸入框，建立時一起送出；每一筆已存在的私人紀錄旁加「編輯」按鈕，展開後可改 label/note 再儲存。用 Playwright 實測完整流程：建立（含 label/note）→ 顯示正確 → 編輯 → 儲存後正確更新 → 刪除，全部正常。

#### Ticket-15　滿版滑動圖集與圖片留言（新增）— ✅ 已修復（2026-09-05）

~~**描述**：多張圖片希望能像社群平台一樣「無邊框、滿版左右滑動（swipeable）」，並支援簡單附上留言／說明文字。~~

**已完成**：`BlockField.images` 型別改成 `(string | GalleryImage)[]`（`GalleryImage = { url, caption? }`），新增 `normalizeGalleryImages()`（`lib/fvals.ts`）統一把舊資料（純字串）跟新資料轉成同一個形狀——**沒有做任何資料遷移**，讀取端統一正規化就夠了。`GalleryLayout` 加第三個值 `'swipe'`：用原生 CSS `scroll-snap`（`overflow-x-auto snap-x snap-mandatory`）做無邊框滿版左右滑動，不用另外接手勢庫，天生不會跟頁面垂直捲動打架；用 `onScroll` 算目前捲到第幾張來驅動下方的圓點指示器。`BlockFileInput`（區塊圖片編輯 UI）每張縮圖旁加了圖說輸入框，`PreviewModal`（燈箱）跟 `GalleryView` 的輪播/滿版滑動都會顯示圖說。後端 `schemas.ts` 的 `blockField.images` 驗證放寬成同時接受字串或 `{url,caption}` 物件，不影響其他驗證規則。

**驗證**：Playwright 實測——新增相簿區塊、上傳兩張圖片網址各附圖說、切到「滿版滑動」呈現、預覽燈箱正確顯示圖說、儲存後直接查 D1 確認存成 `{url,caption}` 物件陣列；到角色公開頁確認滿版滑動版面正確渲染（無邊框、圖說疊在圖片下緣、圓點指示器隨捲動位置更新）。另外用一筆手動塞入舊格式（純字串陣列）`images` 的角色資料確認完全不用遷移、直接正確顯示——相容性符合設計預期。`app/api` 41 個測試全過，前端 `tsc`／`eslint` 跟修改前基準一致。

#### Ticket-16　企劃專屬 Open Graph 預覽圖（整合舊 6、新 11）

**描述**：企劃網址貼到外部（Discord／噗浪）時，預覽圖應動態抓取主辦方上傳的企劃封面圖，不是網站預設 Logo。

**這張票可能已經部分解決，實測後再決定要不要動**：`app/api/src/index.ts` 的 `servePage()` 已經會用 `ogImageOrNull(p.coverUrl)` 動態帶入企劃封面（P2 第一步 path routing 上線時一起做的）；唯一的已知限制是 `ogImageOrNull()` 只認 `http(s)://` 開頭的網址，`data:` URI 封面（使用者直接上傳、還沒接 R2 圖片管線前的常態）會被視為「沒有圖」退回預設 Logo——如果主催上傳的封面是 `data:` URI，這就是預期中的限制，不是新 bug，要解要等 0-2 提過的 R2 圖片管線接上之後才有真正的圖片網址可用。動手前先確認主催回報的情境是不是這一種。

### ⚪ 暫緩／待處理區（Deferred / Future）

#### Ticket-17　收合功能與 Tab 分頁元件（新增）

**描述**：為了讓長篇幅內容更好閱讀，未來需要新增「折疊收合（accordion）」或「Tab 分頁」的排版元件。列入下階段開發清單。

**備註**：`kg.tsx` 已經有 `FillSection`（手風琴摺疊，`kg-acc-head` 樣式）跟 `RowMenu`（"⋯" 選單）這兩個現成元件，Ticket-09 拆掉填寫模式時，這兩個元件在組版模式裡怎麼重用（例如複雜欄位改成手風琴展開而不是跳全螢幕）可以先想，但排入下一階段執行，不在這一輪動工範圍。

---

## P3 — 細節

| 項目 | 說明 |
|---|---|
| 複製邀請連結 | 主催的核心動作是複製貼進 Discord。按鈕放企劃頁最顯眼處，不要藏在管理後台 |
| 空狀態引導 | 新企劃只有主催一人時，動態牆與名單都空。那一刻要顯示「把連結貼給大家就可以開始了」＋複製按鈕 |
| pending 顯示在等誰 | 列表寫「等 ◯◯ 回應」，並給一個複製提醒訊息的按鈕，讓使用者知道該去催誰 |
| 返回鍵關 modal | 手機（尤其 Android）按返回應該關掉 modal，而不是離開整個頁面 |
| 貼上時的隱形字元 | 從 Discord 複製常帶前後空白或零寬字元。`trim()` 之外要濾掉 `\u200B-\u200D\uFEFF` |
| 無障礙補完 | modal 的 `role="dialog"` / `aria-modal` / Escape / focus trap、表單 `htmlFor` 關聯、skip link |

---

## 施工順序建議

1. ~~P0 三項~~ — 已完成
2. ~~1-1 導航、1-2 換頁、1-3 動態牆、1-4 編輯碼救援~~ — 已完成
3. ~~P1.5（私人關係表與關係第三層）~~ — 已完成
4. ~~P2 第一步（path routing + OG meta）、第二步（Discord 登入取代權杖）~~ — 已完成並部署（2026-09-04）
5. **P2.5 上線後實測回報（Ticket-01～17）** — 見上方，🔴 Critical 四項先修，其餘按 🟠→🟡→🟢 順序；⚪ 暫緩區不排進這一輪。
6. **P2 第三、五步（導航拆分、關係圖）** — 部分內容已併入 P2.5 的 Ticket-07/08，其餘照原順序做。
7. **P3** — 隨手做

> 注意：P2 第二步把權杖系統整個換成 Discord 單一身分，跟先前規劃、已作廢並收進 `docs/archive/` 的「Discord 帳號整合（選配連結）」設計是兩個不同方向——後者假設權杖系統繼續存在、Discord 只是額外的一種找回方式；這裡是 Discord 變成唯一身分，權杖被刪除。兩者不能同時要，這份文件走的是後面這一版。
